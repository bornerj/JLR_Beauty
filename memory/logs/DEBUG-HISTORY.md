# Debug History (extraido de memory/MODIFICATION_LOG.md)

# ID: ERR-0054: breadcrumb/sidebar quebrados ao criar `cadastros/clientes` — colidia com o mundo "Clientes" (analytics) já existente ##bug
SINTOMA: achado na validação visual (screenshot, não só o E2E) da Onda 12 do `PLAN-0026` (Cadastro de Clientes nativo). Ao abrir `/admin-v2/cadastros/clientes`, o breadcrumb mostrava "Panorama > CLIENTES > Cadastros > CLIENTES" (duplicado, item do meio sem `onNavigate`) e o item do sidebar destacado era "Clientes" (mundo de analytics do `PLAN-0022`/`PLAN-0023`, Fluxo/Assinaturas) em vez de "Cadastros".
CAUSA_RAIZ: `AdminV2Root.tsx` já tinha `const isCustomersArea = location.pathname.includes("/clientes")`, usado pro mundo de nível superior "Clientes" (`/admin-v2/clientes`, Fluxo/Assinaturas). A Onda 12 criou a rota `cadastros/clientes` (Cadastro de Clientes) — path completo `/admin-v2/cadastros/clientes`, que **também** contém a substring `/clientes`. `.includes()` não distingue "é o mundo de nível superior" de "é uma sub-rota de Cadastros que termina em /clientes" — os dois casaram na mesma checagem, ativando a lógica de breadcrumb/sidebar errada pra tela nova. Nenhuma outra sub-rota de Cadastros colidiu (planos/entrega/cupons/servicos/produtos não reusam nome de nenhum mundo de nível superior) — só "clientes" bateu de propósito com um nome de mundo já existente.
ACAO: `isCustomersArea` trocado de `.includes("/clientes")` pra `/^\/admin-v2\/clientes(\/|$)/.test(location.pathname)` — ancorado ao início do path, só casa o mundo de nível superior de verdade (`/admin-v2/clientes` ou `/admin-v2/clientes/...`), nunca uma sub-rota aninhada sob `/cadastros/`. `isSubscriptionsArea` ganhou `&& isCustomersArea` como guarda defensiva extra (mesmo padrão, redundante mas inofensivo). `docker compose build+recreate web`; revalidado com Playwright + screenshot: breadcrumb "Panorama > Cadastros > CLIENTES" correto, sidebar destaca "Cadastros".
CONTEXTO: 2026-08-16, achado e corrigido durante a execução autônoma do `PLAN-0026` Onda 12 (Clientes, primeira do desmembramento de "Pessoas"), autorização em pé do usuário. **Nota de processo**: ao nomear uma nova sub-rota de Cadastros/Sistema, checar se o slug já é usado por um mundo de nível superior existente (`operacao`, `rede`, `clientes`, `crescimento`, `radar`, `gargalos`, `dinheiro`, `comparador`, `insights`, `sistema`) antes de assumir que `.includes()` é seguro — só "clientes" tinha esse risco até agora, mas o padrão pode se repetir se uma futura onda nomear algo igual a um mundo existente.

# ID: ERR-0053: `DELETE /products/:id` falha com 500 (não 404/409) pra produto com histórico de movimentação de estoque ##bug
SINTOMA: achado no E2E real (não visual) da Onda 11 do `PLAN-0026` (Produtos nativo). Um produto criado com estoque inicial (ou que recebeu qualquer movimento depois) nunca mais pode ser excluído via `DELETE /api/products/:id` — a API responde `500 {"message":"erro interno no servidor"}`, sem detalhe, mesmo comportamento tanto no legado quanto na tela nativa (é 100% backend, `DECISION-014` regra #2 mantém fora de escopo desta onda mudar).
CAUSA_RAIZ: `apps/api/prisma/schema.prisma` — `StockMovement.product` referencia `Product` **sem** `onDelete: Cascade` (comportamento padrão do Postgres/Prisma é `Restrict`). `ProductStock.product` **tem** `onDelete: Cascade` (por isso saldos por unidade não bloqueiam a exclusão), mas o ledger de movimentos (`StockMovement`) sim — qualquer produto com pelo menos 1 linha em `StockMovement` (entrada, uso, perda, ajuste, venda) vira permanentemente não-excluível. O handler `DELETE /products/:id` (`catalog.ts`) só trata `P2025` ("não encontrado") explicitamente; a violação de FK (`P2003`) cai no `catch` genérico e vira 500, não um 409 informativo.
ACAO: nenhuma mudança de código nesta sessão (fora de escopo — mudança de schema/backend). Documentado como achado real confirmado por teste, não suposição: criado produto de teste com `initialStock`, registrada 1 entrada + 1 ajuste, tentada exclusão → `500` confirmado (`{"message":"erro interno no servidor"}`). Produto de teste removido via SQL direto (`DELETE FROM "StockMovement"`/`"ProductStock"`/`"Product"` nessa ordem) já que a API não oferecia caminho — aceitável só porque era dado de teste próprio, nunca faria isso com dado real de usuário. A tela nativa de Produtos já surfaça a mensagem de erro real do backend no `DeleteConfirmModal` (sem fabricar uma explicação melhor do que a real).
CONTEXTO: 2026-08-16, achado durante a execução autônoma do `PLAN-0026` Onda 11 (Produtos), autorização em pé do usuário. **Candidato a fix futuro** (fora desta sessão): adicionar `onDelete: Cascade` em `StockMovement.product` (se apagar o histórico junto for aceitável) OU capturar `P2003` no handler e devolver `409` com mensagem clara ("produto tem histórico de movimentação, não pode ser excluído") em vez de `500` — decisão de produto, não só técnica, então não decidida unilateralmente aqui.

# ID: ERR-0052: modal de "fechar sem salvar" da Galeria de Mídias (Admin V2 nativo) ficava inacessível atrás do modal do editor de slot ##bug
SINTOMA: achado na validação visual (Playwright) da Onda 7 do `PLAN-0026` (Galeria de Mídias nativo), no fluxo de fechar o editor de um slot com alteração pendente. O modal de confirmação (`DeleteConfirmModal` com `tone="neutral"`) abria, mas o clique no botão "Fechar sem salvar" nunca completava — Playwright reportava "element intercepts pointer events", apontando o modal do *editor* (por baixo) como o elemento realmente recebendo o clique.
CAUSA_RAIZ: o modal do editor de slot foi portado do legado com `z-[80]` (valor específico daquele app antigo, onde o admin tinha outros elementos concorrendo por z-index mais alto no shell completo). O `DeleteConfirmModal` compartilhado do Admin V2 usa `z-50` (convenção usada em todos os outros modais nativos — `PlanFormModal`, `CouponFormModal`, `StageChangeReasonModal`). `z-50 < z-[80]`: quando os dois modais coexistem (editor aberto + confirmação de fechar por cima), o de confirmação ficava logicamente "por cima" no DOM mas visualmente atrás na pilha de empilhamento, então recebia clique zero — a caixa do editor, na frente, absorvia o evento.
ACAO: `MediaGalleryView.tsx` — modal do editor trocado de `z-[80]` pra `z-50`, igualando à convenção do resto do Admin V2 (não existe nenhum outro elemento nativo entre 51-79 que justificasse o valor alto herdado do legado). Com z-index igual, a ordem no DOM decide o empate — o `DeleteConfirmModal` é renderizado depois do modal do editor no JSX, então naturalmente pinta por cima. `docker compose build+recreate web`; revalidado com Playwright: clique no "Fechar sem salvar" completa normalmente, com o modal de confirmação de fato clicável por cima do editor.
CONTEXTO: 2026-08-16, achado e corrigido durante a execução autônoma do `PLAN-0026` Onda 7 (Galeria de Mídias), autorização em pé do usuário. **Nota de processo**: ao portar um modal do legado que usa `z-[N]` arbitrário, sempre conferir contra a convenção `z-50` já estabelecida no Admin V2 pra qualquer novo modal nativo — não copiar o valor do legado sem checar se ele ainda faz sentido no contexto isolado do V2.

# ID: ERR-0051: toggle switch de Seções Telas (Admin V2 nativo) renderizava sem cor nenhuma — mesma causa raiz do ERR-0049/ERR-0040 ##bug
SINTOMA: achado na validação visual (screenshot, não só o E2E via `curl`) da Onda 6 do `PLAN-0026` (Seções Telas nativo). Todos os 32 toggles apareciam brancos/sem cor e com o círculo sempre à esquerda, **mesmo pra seções com `enabled: true` no banco** (confirmado via `GET /admin/section-toggles` que os 32 valores reais de produção eram todos `true`). Pixel-sampling (Python/PIL, lição do `ERR-0049` — nunca confiar só em "parece certo") confirmou: fundo do pill sólido branco `(255,255,255)`, nenhum verde em lugar nenhum.
CAUSA_RAIZ: idêntica ao `ERR-0049`/`ERR-0040` — `tailwind.generated.css` é uma foto estática de "quais classes Tailwind estavam em uso quando o arquivo foi gerado" (2026-08-15, Onda 5 do `PLAN-0025`), não um build ao vivo. O componente novo (`SectionTogglesView.tsx`) introduziu classes que **nunca tinham sido usadas em lugar nenhum do código até esta onda**: `border-state-healthy` na forma *bare* (só existia com sufixo de opacidade, `\/30`, antes), e 2 valores arbitrários (`w-[52px]`, `left-[26px]`) pro switch customizado. Nenhuma dessas 3 classes existia no CSS servido — o navegador simplesmente ignora a regra referenciada, sem erro, então o `<span>` do pill ficava sem `background-color` (mostrando o branco do card por trás) e o `<span>` do knob ficava sem `left` explícito (caía na posição estática, sempre à esquerda), **independente do valor de `enabled`**. Confirma o padrão: qualquer classe Tailwind nova (não usada antes em nenhum arquivo) introduzida por uma tela nativa do Admin V2 tem esse risco — não é exclusivo dos tokens `state-*`, é qualquer classe ausente do snapshot.
ACAO: regeneração completa do `tailwind.generated.css` via o comando já documentado no cabeçalho do próprio arquivo (`npx tailwindcss@3.4.17 -c tailwind.config.js -i ... -o apps/web/src/styles/tailwind.generated.css`, rodado da raiz do repo) — o comando rescaneia TODO o código-fonte atual (`content` globs do `tailwind.config.js`), não só o que motivou a geração anterior, então automaticamente pega classes novas de todas as Ondas 1-6 do `PLAN-0026`, não só as do toggle. Arquivo cresceu de 5204 pra 5277 linhas; CSS final do build cresceu de 110.68kB pra 111.77kB (confirma que novas regras entraram). Cabeçalho do arquivo atualizado com a data e o achado desta rodada. `docker compose build+recreate web`; revalidado com Playwright + pixel-sampling: `rgb(0,150,127)` exato (`state-healthy`) no pill ligado, cinza claro + knob à esquerda no desligado — confirmado clicando um toggle ao vivo (frame intermediário da transição CSS capturado por engano na primeira tentativa de screenshot foi descartado como falso-alarme, ver nota de processo abaixo).
CONTEXTO: 2026-08-16, achado e corrigido durante a execução autônoma do `PLAN-0026` Onda 6 (Seções Telas), autorização em pé do usuário. **Nota de processo pra próximas ondas**: sempre que uma tela nativa introduzir um padrão visual genuinamente novo (switch, dropzone de upload, drag handle, etc. — não só reusar classes já usadas em ondas anteriores), regenerar este arquivo faz parte da validação padrão, não só quando um bug aparecer — mais barato que descobrir via pixel-sampling toda vez.

# ID: ERR-0050: criação de cupom de desconto nativo (Admin V2) falhava com "dados invalidos" ao trocar o tipo de desconto ##bug
SINTOMA: achado na validação visual real (Playwright) da Onda 4 do `PLAN-0026` (Cupons de Desconto nativo) — não no RAG estático nem no E2E via `curl`, que passou porque não exercitava o mesmo payload que a UI gera. Criar um cupom do zero pela tela nova sempre falhava com "dados invalidos" (400), mesmo com todos os campos obrigatórios preenchidos corretamente (código, nome, tipo Percentual, percentual 15).
CAUSA_RAIZ: `apps/api/src/routes/admin.ts` tem 2 schemas Zod diferentes pra Cupons — `discountCouponSchema` (criação) só aceita `number | undefined` em `percentOff`/`amountOff`/`minSubtotal`, **não `null`**; só `discountCouponUpdateSchema` (edição) aceita `null` explícito nesses 3 campos (pra permitir limpar um valor já setado). `z.coerce.number()` aplicado a `null` produz `0` (não lança erro de coerção), que então reprova a regra `min(0.01)` do campo — daí "dados invalidos" sem pista de qual campo. O `CouponFormModal.tsx` novo mandava `null` explícito pro campo de desconto não usado (`amountOff: null` quando o tipo era PERCENT, por ex.) em **ambos** os casos, criar e editar — quebrando toda criação. O form legado (`AdminDiscountCouponsView.tsx`) já fazia essa distinção corretamente (`payload.amountOff = isUpdate ? null : undefined`), mas ela não foi replicada na reescrita nativa na primeira tentativa.
ACAO: `CouponFormModal.tsx` passou a diferenciar `clearValue = isEditing ? null : undefined` — o campo de desconto oposto ao tipo escolhido (e o subtotal mínimo vazio) usa esse valor em vez de sempre `null`. `undefined` faz `JSON.stringify` omitir a chave inteiramente, que é exatamente o que o schema de criação espera; `null` explícito na edição segue permitindo limpar o campo/trocar de tipo (o backend já zera a coluna oposta no banco quando o tipo muda, ver `mergedAmountOff`/`mergedPercentOff` em `admin.ts`, então nem dependia estritamente do `null` — mas mantido por fidelidade ao contrato do schema de update). `docker compose build+recreate web` (revalidação completa); Playwright (8 checks) confirmou o fluxo completo depois do fix: criar → aparece na tabela → persiste após reload → editar → excluir → estado vazio confirmado por reload, banco sem dado de teste deixado pra trás.
CONTEXTO: 2026-08-16, achado e corrigido durante a execução autônoma do `PLAN-0026` Onda 4 (Cupons), autorização em pé do usuário pra prosseguir sem pausa entre ondas.

# ID: ERR-0049: tokens semânticos `state-*` nunca compilados no CSS servido desde a Onda 1 do PLAN-0022 ##bug
SINTOMA: achado durante o item 6 do `PLAN-0025` (contraste da Agenda-Capacidade) — mesmo depois de aumentar as opacidades em `state.ts`, a grade continuava mostrando 0% em fundo branco. Investigação por pixel (Python/PIL num screenshot Playwright) confirmou: célula de 0% renderizava `(247,255,255)` — praticamente idêntico ao branco de fundo `(248,252,249)` — quando devia ser um salmão claro. `grep` nos 2 CSS pré-compilados do projeto (`tailwind.css`, `tailwind.react.patch.css`) por `state-critical`/`state-attention`/`state-healthy`/`state-info` (os 4 tokens semânticos criados especificamente pro Admin V2, `DECISION-013` regra #6) deu **zero ocorrências, em qualquer forma** — nem a cor base, nem nenhuma variação de opacidade. Confirmado visualmente em outra tela sem relação (Insights, Panorama) que as bolinhas de estado (`bg-state-healthy` etc.) também não tinham cor nenhuma.
CAUSA_RAIZ: o projeto usa CSS Tailwind **pré-compilado e commitado** (mesma causa raiz do `ERR-0040`, mas de escopo sistêmico aqui) — `npm run build` (`tsc -b && vite build`) e o `Dockerfile` do `web` nunca rodam um passo de build do Tailwind; os 2 arquivos CSS existentes são hand-maintained. Os tokens `state-*` foram adicionados ao `tailwind.config.js` na Onda 1 do `PLAN-0022` (2026-08-13) mas **ninguém rodou/re-patchou o CSS pra incluí-los** — toda cor de estado (Health Score, badges de prioridade do Insight Engine, severidade do Radar/Gargalos, bordas de gargalo, contraste da Agenda-Capacidade) ficou silenciosamente sem cor desde então, em múltiplas sessões, sem nenhum erro de build/lint/teste acusar (Tailwind "silenciosamente" não gera erro pra uma classe referenciada que não existe no CSS — ela só não aparece).
ACAO: confirmado que `npx tailwindcss@3.4.17` roda neste ambiente sem instalação prévia (fetch on-demand). Tentativa inicial de **substituir** os 2 arquivos pelo output do Tailwind CLI foi **abortada**: comparação classe-a-classe mostrou que os arquivos atuais têm CSS customizado hand-written misturado junto (`.metric-card`, `.footer-*`, `.nav-*`, `.brand-*`, `.flip-card`, `.display-*`, etc.) que `tailwindcss build` não reproduz — substituir teria apagado esse CSS e quebrado o site público. Fix aplicado (aditivo, zero risco pro CSS existente): gerado `apps/web/src/styles/tailwind.generated.css` (só `@tailwind utilities`, sem Preflight — evita reset duplicado) contendo as 161 classes que estavam em uso no código mas ausentes do CSS servido (os 4 tokens `state-*` completos + outras ~150 classes de gaps acumulados de outras sessões — `ring-2`, `disabled:cursor-not-allowed`, `animate-spin`, valores arbitrários como `max-h-[90vh]`, etc.), importado por último em `main.tsx`. Classes Tailwind são idempotentes — uma regra repetida no arquivo novo e nos antigos é inofensiva. Comando de regeneração documentado no cabeçalho do próprio arquivo novo, pra não repetir esse levantamento manual numa sessão futura.
CONTEXTO: 2026-08-15, achado e corrigido durante a execução do `PLAN-0025` (item 6), a pedido explícito do usuário depois de eu reportar o achado e propor a estratégia aditiva (troca completa dos arquivos foi cogitada e rejeitada por risco). Validado com checagem de pixel real via Playwright + PIL (não confiar em "parece certo" visualmente — a lição desta investigação) em 3 telas: Agenda-Capacidade, Panorama (bolinhas de rede) e Insights (badges de prioridade, cards de impacto).

# ID: ERR-0048: `revenueTrendPercent` caía pra 0% (flat) com receita anterior zero, subestimando o Health Score ##bug
SINTOMA: achado #9 do `/code-review high` (revisão pré-merge do PR #1), investigado a pedido do usuário após o merge. Confirmado com dados reais: `GET /api/admin-v2/network` mostrava `revenueTrendPercent: 0` pra todas as 5 unidades (a massa de teste não tem pedidos no período anterior aos últimos 30 dias); `GET /api/admin-v2/panorama` mostrava "Receita 0.0%" na tela mesmo com R$ 11.290,00 de receita no período atual. Durante a investigação, achado o MESMO bug duplicado em `panorama/service.ts` — não fazia parte do achado original do review, só apareceu ao investigar a fundo.
CAUSA_RAIZ: `apps/api/src/modules/intelligence/unit-health/service.ts` e `apps/api/src/modules/intelligence/panorama/service.ts` — os dois calculavam `revenueTrendPercent = revenuePrevious > 0 ? ((atual-anterior)/anterior)*100 : 0`. Quando `revenuePrevious` é 0, a divisão não existe; tratar isso como "0% = flat" é semanticamente errado (0% comunica "não mudou nada", quando na verdade não há base de comparação) e alimentava `normalizeGrowth()` (`clamp(50 + trend*2)`) com um valor neutro (score 50) em vez de refletir o crescimento real — subestimando o componente de Crescimento (peso 20%) do Health Score pra qualquer unidade no primeiro período ou com receita anterior genuinamente zero.
ACAO: usuário escolheu explicitamente o tratamento (`+25%` como piso, não `null`): quando não há receita anterior mas há receita atual, usa `25` (o menor valor que já satura `normalizeGrowth` — nota 100 — sem fingir saber o percentual exato de um crescimento partindo de zero); quando as duas receitas são zero, `0%` (flat) continua correto. Aplicado nos dois arquivos, com comentário inline explicando o raciocínio pra não ser revertido sem essa memória. `npm run test` (api) 134/134 PASS; `docker compose build+recreate api`; E2E real confirmou: as 5 unidades passaram a mostrar `revenueTrendPercent: 25` (Health Score subiu ~10 pontos em cada, coerente com o componente de Crescimento saturando); regressão OK em todos os 7 endpoints de inteligência; validação visual real confirmou "Receita +25.0%" no Panorama (era "0.0%").
CONTEXTO: 2026-08-15, investigado e corrigido na sessão seguinte ao merge do PR #1, a pedido explícito do usuário ("investiga os achados #8 e #9 do review").

# ID: ERR-0047: `ordersNeedingAttention` do Panorama ignorava o período pedido, sempre usava "agora" ##bug
SINTOMA: achado #8 do `/code-review high` (revisão pré-merge do PR #1), investigado a pedido do usuário após o merge. Confirmado com dados reais: `GET /api/admin-v2/panorama?days=30` (janela terminando hoje) devolvia `ordersNeedingAttention: 28`; `GET /api/admin-v2/panorama?from=2020-01-01&to=2020-01-31` (período histórico arbitrário, aceito pela API via querystring) devolvia o mesmo tipo de contagem baseada em "agora" em vez de refletir o período pedido — inconsistente com `financial`/`customers`/`network`, que respeitam `range.from`/`range.to` corretamente.
CAUSA_RAIZ: `apps/api/src/modules/intelligence/panorama/service.ts` — `countOrdersNeedingAttention()` calculava `staleBefore` a partir de `Date.now()`, nunca do período (`range.to`) que a própria função `getPanorama` já tinha resolvido. A UI atual só manda janelas rolantes terminando hoje (por isso o bug nunca apareceu visualmente em uso normal), mas a rota aceita `from`/`to` arbitrários — qualquer chamada com período histórico real ficava com esse campo mostrando o estado de HOJE, não do período pedido.
ACAO: `countOrdersNeedingAttention` passou a receber `referenceDate` (chamada com `range.to`) em vez de usar `Date.now()` internamente. Limitação documentada explicitamente (`Order` não guarda histórico de `fulfillmentStatus`, então não é uma reconstrução histórica perfeita — só garante que o campo nunca mais ignora o período pedido). Doc comments adicionados em `panorama/types.ts` explicando os 2 campos period-agnostic por natureza (`stockAlerts`, estado de estoque não tem "histórico" sem event-sourcing) vs. o que foi corrigido. `npm run test` (api) 134/134 PASS; `docker compose build+recreate api`; E2E real confirmou: período normal continua `28` (sem regressão), período histórico (`2020-01`) passou a devolver `0` (antes devolveria `28` incorretamente).
CONTEXTO: 2026-08-15, investigado e corrigido na sessão seguinte ao merge do PR #1, a pedido explícito do usuário ("investiga os achados #8 e #9 do review").

# ID: ERR-0046: `columnFor` podia esconder pedido BLOCKED atrás do status PENDENTE no board operacional ##bug
SINTOMA: achado em `/code-review high` (revisão pré-merge do PR #1, `main...feature/admin-v2`), não observado em produção — nenhum pedido real caiu nesse caso porque o único write path que hoje seta a tag `[ESTOQUE]` em `fulfillmentNotes` também sempre grava `status: PAGO` na mesma escrita.
CAUSA_RAIZ: `apps/api/src/modules/intelligence/operational-orders/service.ts` — `columnFor()` checava `order.status === "PENDENTE"` (coluna "entraram") ANTES de checar `card.operationalState !== "NORMAL"` (coluna "atencao"). Um pedido com `status: PENDENTE` e nota `[ESTOQUE]` é classificado `BLOCKED` por `classifyOrder()` (que roda incondicionalmente), mas `columnFor` o mandava pra "entraram" mesmo assim — contrariando o próprio comentário da função ("atencao tem prioridade sobre o estágio natural").
ACAO: invertida a ordem das duas checagens — `operationalState !== "NORMAL"` agora vem primeiro. `npm run test` (api) 134/134 PASS (sem teste unitário dedicado pra `columnFor`, mesmo padrão dos demais `service.ts` Prisma-dependentes — só E2E real); E2E real confirmou o board sem regressão (43 pedidos, mesma contagem de antes do fix) após rebuild Docker.
CONTEXTO: 2026-08-15, achado durante a revisão pré-merge do PR #1 (`/code-review high`), corrigido na mesma sessão antes do merge.

# ID: ERR-0045: drill-down "Ver agenda" do Diagnóstico da Unidade perdia o filtro de unidade ##bug
SINTOMA: achado em `/code-review high` (revisão pré-merge do PR #1), confirmado via Playwright: clicar em "Ver agenda"/"Ver produtos"/"Comparar unidade" no Diagnóstico da Unidade (`/admin-v2/rede/:unitId`) navegava para a tela certa, mas sem a unidade selecionada — `CapacityView` caía no estado "selecione uma unidade" em vez de já abrir filtrada.
CAUSA_RAIZ: `apps/web/src/admin-v2/network/UnitDetailView.tsx` — o `onClick` do botão fazia duas chamadas em sequência: `setUnitId(unit.unitId)` (que internamente já é uma navegação — `setSearchParams` pra `?unit=X` na URL atual) seguida de `navigate(action.path)` (path sem nenhuma query string). A segunda navegação vencia e descartava o `?unit=` que a primeira acabara de setar.
ACAO: substituídas as duas chamadas por uma única `navigate(\`${action.path}?unit=${unit.unitId}\`)`. `setUnitId` removido do destructure de `useAdminScope()` (não usado mais neste arquivo). Validado via Playwright real: clique em "Ver agenda" agora resulta em `/admin-v2/operacao/agenda?unit=1`, dropdown de unidade já mostra "Parque da Cidade", `CapacityView` não cai mais no estado vazio.
CONTEXTO: 2026-08-15, achado durante a revisão pré-merge do PR #1 (`/code-review high`), corrigido na mesma sessão antes do merge.

# ID: ERR-0044: `npm ci` com ETIMEDOUT dentro do build Docker (api/web) — IPv6 sem rota ##bug
SINTOMA: `docker compose up -d --build api web` falhava em `RUN npm ci` com `npm error code ETIMEDOUT` / `npm error errno -110` / `network read`, após ~60s. Pull das imagens base (postgres/node) funcionava, só o `npm ci` falhava. Tentativa do usuário de atualizar `npm` no host (`npm install -g npm@12.0.1`) não tinha relação com o build (o build usa o npm de dentro da imagem `node:20-slim`/`node:20-alpine`, não o do host) e falhou por incompatibilidade de engine (irrelevante ao problema real).
CAUSA_RAIZ: `getent hosts registry.npmjs.org` no host e em `node:20-slim` retorna só endereços IPv6 (Cloudflare). A rede Docker (`bridge`) está com `EnableIPv6: false`, então qualquer tentativa de conexão IPv6 fica sem rota e trava até estourar timeout. Confirmado empiricamente de dentro do container: `curl -6` → timeout total (~7,2s, sem resposta); `curl -4` → 200 OK em 0,17s. O Node por padrão tenta resolver/conectar sem forçar IPv4, então `npm ci` (e potencialmente `npx prisma generate`, que baixa engine binaries) ficava preso na rota IPv6 morta até esgotar as tentativas.
ACAO: Adicionado `ENV NODE_OPTIONS=--dns-result-order=ipv4first` em ambos os estágios (`build`/`prod`) de `apps/api/Dockerfile` e no estágio `build` de `apps/web/Dockerfile`, forçando o Node a preferir IPv4 na resolução de DNS. Validado com rebuild completo (`docker compose up -d --build api web`): build passou, containers `api`/`web`/`postgres` subiram saudáveis. Mantidas as mitigações já existentes no working tree antes deste fix (retry `fetch-retries`/`fetch-retry-mintimeout`/`fetch-retry-maxtimeout` e `--mount=type=cache` no `npm ci`), que sozinhas não resolviam a causa raiz (retry contra uma rota sem resposta só adia o timeout).
CONTEXTO: 2026-07-21/22; ambiente dev local (Zorin Linux, disco secundário); Docker 29.1.3 sem `buildx` instalado (warning "Bake" no `docker compose up --build`, não relacionado à causa raiz); `node:20-slim` (api) e `node:20-alpine` (web); rede `docker0`/bridge padrão sem IPv6 habilitado.

# ID: ERR-0043: PostgreSQL container Exited(127) após reboot — API em crash-loop P1001 ##bug
SINTOMA: `jlr_beauty-postgres-1` com status `Exited (127)` por ~23h após reboot da máquina; `jlr_beauty-api-1` em crash-loop com `Error: P1001: Can't reach database server at postgres:5432`; SPA sem produtos, admin inoperante.
CAUSA_RAIZ: O projeto vive em disco secundário (`/media/jeiel/A8FEADE5FEADABCE14/...`). No boot, o daemon Docker inicia antes do ponto de montagem do disco estar disponível; o bind mount de arquivo único do compose (`./docker/postgres/init-api-users.sh:/docker-entrypoint-initdb.d/01-...`) resolve para caminho inexistente → entrypoint falha com exit 127 (command/file not found) e o container não se recupera. A policy `restart: unless-stopped` está correta e NÃO é a causa.
ACAO: `docker compose up -d postgres` (manual) recuperou tudo — banco `healthy`, API voltou sozinha, endpoint público devolvendo os 8 produtos. Sem mudança no compose (problema é ordem de montagem do host em ambiente dev local; em VPS de produção o projeto vive no disco raiz e o cenário não ocorre). Mitigação operacional documentada: após reboot da máquina dev, conferir `docker ps` e subir o compose se necessário.
CONTEXTO: 2026-07-06; PLAN-0020 Onda 0; docker-compose.yml postgres bind mount; Linux 6.17 + Docker; disco NTFS /media automontado após o daemon.

# ID: ERR-0042: CORS bloqueando fetch same-origin do próprio frontend (SEC-27) ##bug
SINTOMA: Ao restringir o CORS da API para exigir header `Origin` em produção (exceto webhooks/health checks), um teste com Chrome real em modo headless (`fetch('/api/public/products')` a partir de página servida pela mesma origem nginx) retornou `403 acesso negado` — o próprio fetch same-origin do frontend real ficaria bloqueado em produção.
CAUSA_RAIZ: Suposição incorreta de que "toda chamada do SPA sempre envia `Origin`". Nesta topologia, web e api são servidos pela MESMA origem via nginx (`APP_WEB_URL`/`CORS_ORIGIN` apontam para o mesmo host:porta que serve `/api/`). Browsers modernos (confirmado empiricamente com Chrome headless) não enviam o header `Origin` em um `fetch` GET same-origin. `curl` sem header simula esse caso mas não prova o comportamento real do browser — só o teste com browser revelou o problema antes de ir para produção.
ACAO: Revertido o bloqueio em `apps/api/src/app.ts`, restaurando `!origin => allow` (comportamento original). Mantido apenas um comentário no código documentando a investigação e o motivo de não reimplementar isso sem repetir o teste com browser real. Ver DECISION-012 para o registro formal da decisão de não perseguir esse fix nesta arquitetura.
CONTEXTO: 2026-07-05; PLAN-0018 Onda 3 (SEC-27); Express 5 + cors package; deploy nginx reverse-proxy same-origin (web+api no mesmo host:porta); validado com `google-chrome --headless=new --dump-dom`.

# ID: ERR-0041: HMAC de rastreamento de pedido retorna 500 em vez de 401 com token de tamanho inválido (SEC-21) ##bug
SINTOMA: `GET /public/orders/track/:publicCode?hmac=<valor_curto>` retornava `500 erro interno no servidor` em vez do `401 Invalid HMAC` esperado, quando o atacante envia um HMAC de tamanho diferente de 64 chars hex (ex.: um valor curto ou aleatório de outro tamanho).
CAUSA_RAIZ: `crypto.timingSafeEqual(bufferA, bufferB)` do Node lança `ERR_CRYPTO_TIMING_SAFE_EQUAL_LENGTH` quando os dois buffers comparados têm tamanhos diferentes, em vez de retornar `false`. A função `verifyOrderHmac` não tratava esse caso, deixando a exceção subir e cair no handler de erro genérico (500).
ACAO: Em `apps/api/src/lib/hmacUtils.ts`, adicionado guard de comprimento antes de chamar `timingSafeEqual`: se os buffers tiverem tamanhos diferentes, retorna `false` imediatamente. O comprimento do HMAC não é segredo (SHA-256 hex sempre tem 64 chars), então esse guard não introduz vazamento de informação útil ao atacante.
CONTEXTO: 2026-07-05; PLAN-0018 Onda 1 (SEC-21); Node.js `crypto` nativo; encontrado durante penetration test simulado via curl contra ambiente Docker real, antes do fechamento da Onda 1.

# ID: ERR-0040: Tailwind CSS pré-compilado — classes novas não refletem sem rebuild Docker ##bug
SINTOMA: Classes Tailwind adicionadas ao TSX (`grid-cols-5`, `aspect-square`) não tinham efeito visual mesmo após Ctrl+Shift+R. Imagens empilhavam verticalmente e ficavam em tamanho errado.
CAUSA_RAIZ: O projeto usa CSS Tailwind pré-compilado commitado no repositório (`tailwind.react.patch.css`, `tailwind.css`). O Vite roda DENTRO do container Docker — não há dev server local. Classes novas que não estavam no CSS compilado simplesmente não existiam na build servida.
ACAO: 1) Adicionadas as classes faltantes manualmente ao `tailwind.react.patch.css`. 2) Layout reescrito com `style` inline no React para garantir independência do CSS compilado. 3) Usuário executou `docker compose up -d --build web` para rebuild do container.
CONTEXTO: 2026-06-22; Zorin Linux; Chrome; projeto rodando inteiramente em Docker (sem npm run dev local).

# ID: ERR-0001: Erro 500 no login por dependencia de schema acessorio
SINTOMA: `POST /auth/login` retornando erro 500 ("correcao de erro 500 no /auth/login").
CAUSA_RAIZ: Fluxo de login dependia de colunas alem do minimo; divergencias de schema/coluna auxiliar podiam derrubar a autenticacao.
ACAO: Em `apps/api/src/routes/index.ts`, login passou a usar `findFirst` com `select` minimo (`id`, `name`, `email`, `role`, `passwordHash`), `lastAccessAt` nao-bloqueante com `catch`, e `logger.error` no `catch` principal.
CONTEXTO: 2026-02-14; Node v20.19.5; Express 5.2.1; Prisma 5.22.0; Windows (Laragon local).

# ID: ERR-0002: Falha de startup Prisma com mensagem `URL must start with prisma://`
SINTOMA: API falhando no startup ao executar `prisma.conciergeSession.deleteMany()` com erro `URL must start with prisma://` / datasource exigindo `prisma://`.
CAUSA_RAIZ: Prisma Client gerado com `--no-engine` + variaveis `PRISMA_*` conflitantes na sessao de terminal.
ACAO: Regeneracao com `npx prisma generate` em `apps/api` e limpeza de variaveis `PRISMA_*` no atalho global `C:\Users\Jeiel\start_backend.bat`.
CONTEXTO: 2026-02-14 e 2026-02-15; Prisma 5.22.0 + MySQL local; Windows.

# ID: ERR-0003: Erro P2022 na grid de profissionais
SINTOMA: Rota `/professionals` falhando com Prisma `P2022` (coluna `Professional.employmentStatus` inexistente), refletindo erro 500 na tela.
CAUSA_RAIZ: Migration `20260215001500_professional_commission_profile_and_lifecycle` pendente no MySQL local.
ACAO: `npx prisma migrate deploy` em `apps/api` para aplicar migration; validacao posterior com consulta Prisma (`count=6`).
CONTEXTO: 2026-02-15; Node v20.19.5; Prisma 5.22.0; MySQL local (Laragon).

# ID: ERR-0004: Endpoint de WhatsApp retornando 500 apenas na instancia ativa
SINTOMA: `POST http://localhost:3001/api/public/concierge/whatsapp-summary` retornava `500` (`{"message":"erro interno no servidor"}`), enquanto processos novos retornavam `202`.
CAUSA_RAIZ: Instancia ativa em `:3001` estava com ambiente desatualizado (necessitava reload de `.env`).
ACAO: Encerrada instancia antiga e reiniciada API na porta `3001`; validacao apos restart retornou `202` com `{ "success": true }`.
CONTEXTO: 2026-02-12; API local em Node/Express; integracao Z-API.

# ID: ERR-0005: Z-API com `200` HTTP e erro logico `NOT_FOUND`
SINTOMA: Integracao recebia resposta HTTP `200` com erro logico no payload (`NOT_FOUND`); tambem havia falha por proxy local `127.0.0.1:9`.
CAUSA_RAIZ: `ZAPI_BASE_URL` preenchido com URL completa de instancia/token/send-text e validacao anterior considerava apenas status HTTP.
ACAO: Em `apps/api/src/lib/zapi.ts`, normalizacao de URL para `/send-text`, suporte a `ZAPI_BASE_URL` completa e validacao de erro logico no body; em `send_message.php`, proxy local desabilitado com `CURLOPT_PROXY/CURLOPT_NOPROXY`.
CONTEXTO: 2026-02-12; API Express 5.2.1; ambiente local Windows + cURL/PHP.

# ID: ERR-0006: Falha de ambiente `spawn EPERM` em reexecucao de spec Playwright
SINTOMA: Reexecucao isolada do spec falho nao concluida por `spawn EPERM`.
CAUSA_RAIZ: Restricao/instabilidade de ambiente de execucao no Windows durante teste E2E.
ACAO: Sem patch de codigo aplicado no registro; acao operacional foi adiar e estabilizar runner antes de repetir o E2E.
CONTEXTO: 2026-02-07; Web com Playwright 1.58.1; Windows local.

# ID: ERR-0007: `prisma generate` falhando com lock de DLL (`EPERM`)
SINTOMA: `npx prisma generate` falhou com `EPERM` em `query_engine-windows.dll.node`.
CAUSA_RAIZ: Processo Node/backend ativo segurando arquivo da engine Prisma.
ACAO: Procedimento operacional definido: parar backend antes de `prisma generate`; em janela critica, geracao sem engine foi usada para nao interromper servico.
CONTEXTO: 2026-01-30, 2026-02-15 e 2026-02-17; Prisma 5.22.0; Windows.

# ID: ERR-0008: Erro 500 generico em operacoes de produto
SINTOMA: Endpoints de produto retornavam `erro interno no servidor` (500) em falhas de referencia (categoria/status).
CAUSA_RAIZ: Ausencia de tratamento especifico para erros Prisma conhecidos (ex.: `P2003`, `P2025`).
ACAO: Em `apps/api/src/routes/index.ts`, normalizacao de IDs opcionais, validacao previa de referencias, e `try/catch` com tratamento de `P2003/P2025` em `POST/PATCH/DELETE /products`.
CONTEXTO: 2026-02-17; Express 5.2.1; Prisma 5.22.0; API local.

# ID: ERR-0009: Checkout travando em `Aplicando...` no cupom
SINTOMA: Botao de cupom podia ficar indefinidamente em `Aplicando...` quando validacao nao retornava (timeout/rede/erro backend).
CAUSA_RAIZ: Front sem timeout/abort robusto e endpoint publico sem `try/catch` para falhas inesperadas.
ACAO: Em `apps/web/src/components/pages/CheckoutContent.tsx`, adicao de `AbortController` com timeout (`12000ms`) e `finally` para reset; em `apps/api/src/routes/index.ts`, `POST /api/public/discount-coupons/validate` encapsulado em `try/catch` com log e resposta padrao.
CONTEXTO: 2026-02-18; React 19.2.0 + Vite 7.2.4; API Express/Prisma.

# ID: ERR-0010: Falha de build no Vercel por duplicacao de path
SINTOMA: Build quebrava com path invalido `apps/web/apps/web/package.json`.
CAUSA_RAIZ: Root Directory configurado em `apps/web` e `vercel.json` ainda usando comandos com `--prefix apps/web`.
ACAO: Ajuste de `vercel.json`: `installCommand` -> `npm install`, `buildCommand` -> `npm run build`, `outputDirectory` -> `dist`.
CONTEXTO: 2026-02-19; deploy Vercel; frontend Vite.

# ID: ERR-0011: Erro 404 em rotas SPA no Vercel (`/admin`)
SINTOMA: Login funcionava, mas abrir rota SPA (`/admin`) retornava `404 NOT_FOUND`.
CAUSA_RAIZ: Ausencia de rewrite de fallback para `index.html` no deploy.
ACAO: Em `apps/web/vercel.json`, adicionado rewrite global `source: "/(.*)"` para `destination: "/index.html"`.
CONTEXTO: 2026-02-19; Vercel + SPA React Router.

# ID: ERR-0012: Modal "Ver Menu Completo" alto e conteudo vazio apos erro inicial
SINTOMA: Modal nao cabia na viewport e o conteudo podia nao reaparecer apos primeira falha de carregamento.
CAUSA_RAIZ: Layout com `h-full` gerando colapso/overflow e estado `error` persistente sem rearmar fetch ao reabrir.
ACAO: Em `apps/web/src/modules/public-site/sections/HomeServicesSection.tsx`, `openCatalogModal()` passou a resetar para `idle` quando estado anterior era `error`; modal com `max-h` responsivo e area de conteudo com `min-h-0`.
CONTEXTO: 2026-02-24; React 19.2.0; Vite 7.2.4.

# ID: ERR-0013: Erro de relacao ao criar agendamento no chatbot web
SINTOMA: Erro interno ao gravar cliente/agendamento em `POST /public/concierge/book`.
CAUSA_RAIZ: `appointment.clientId` recebia `Customer.id`, mas `Appointment.clientId` referencia `User.id`.
ACAO: Em `apps/api/src/routes/index.ts`, rota passou a usar `customer.userId ?? parsed.data.clientId` para `clientId`, mantendo lookup/upsert de `Customer` por telefone.
CONTEXTO: 2026-02-24; API Express 5.2.1 + Prisma 5.22.0; Railway/local.

# ID: ERR-0014: Erro interno em `GET /api/admin/section-toggles`
SINTOMA: Tela de toggles no Admin abria com erro interno; endpoint podia retornar `500`.
CAUSA_RAIZ: Resolucao de path de `sectionToggles.ts` dependente de `process.cwd()`, variando entre ambientes (local/Railway).
ACAO: Em `apps/api/src/routes/index.ts`, troca para resolver robusto com multiplos caminhos candidatos (`cwd` + `__dirname`), `fs.existsSync`, e diagnostico com paths testados.
CONTEXTO: 2026-02-24; API no Railway/local; Node v20.19.5.

# ID: ERR-0015: Erro de compilacao TS apos extracao do admin shell
SINTOMA: Build com erro `Cannot find name setActiveView` apos extracao de modulo.
CAUSA_RAIZ: `setActiveView` nao estava sendo exportado/reconectado entre modulo `admin-shell` e camada legacy.
ACAO: `setActiveView` foi exportado no retorno de `initAdminShellBehavior` e reconectado em `apps/web/src/legacy/admin.behavior.ts`.
CONTEXTO: 2026-02-16; TypeScript 5.9.x; React 19.2.0.

# ID: ERR-0016: `prisma migrate deploy` bloqueado por `P3005` (schema sem baseline)
SINTOMA: `npx prisma migrate deploy` falhou com erro `P3005` (schema existente sem baseline).
CAUSA_RAIZ: Banco alvo ja possuia estrutura previa sem baseline de migrations.
ACAO: Acao registrada como pendente: aplicar estrategia de baseline no banco local correto (Laragon) antes de usar endpoints novos em runtime.
CONTEXTO: 2026-03-01; Prisma 5.22.0; MySQL local (Laragon).

## Erros Extraidos dos Contextos de Conversa (sessoes Codex)

# ID: ERR-0017: Timeout recorrente em buscas/leitura de arquivos grandes
SINTOMA: Mensagens de sessao como "As buscas deram timeout pelo tamanho dos arquivos" e "comandos estouraram timeout do ambiente".
CAUSA_RAIZ: Consultas amplas em arquivos/logs extensos com timeout padrao curto para o volume de dados.
ACAO: Reexecutar em comandos menores e aumentar timeout; separar leitura por blocos/arquivos especificos.
CONTEXTO: Sessoes Codex em Windows/PowerShell, workspace `c:\laragon\www\JLR_AI_Studio`.

# ID: ERR-0018: Falha `ENOTCACHED` ao executar compilacao Tailwind via `.bat`
SINTOMA: Execucao de `compile_tailwind.bat` falhou com erro de cache offline do npm (`ENOTCACHED`).
CAUSA_RAIZ: Ambiente sem acesso/entrada de cache necessaria para `npx tailwindcss@...` no momento da execucao.
ACAO: Script foi mantido funcional; tratar como incidente operacional (garantir cache/rede ou dependencia local instalada) e rerodar.
CONTEXTO: Sessao 2026-02-07; Node/npm em Windows local.

# ID: ERR-0019: Falha de comando por quoting do caminho do `pwsh`
SINTOMA: "Os comandos falharam por quoting do caminho do `pwsh`" durante validacao.
CAUSA_RAIZ: Escape/quoting incorreto ao montar comando com caminho contendo espacos (`C:\Program Files\PowerShell\7\pwsh.exe`).
ACAO: Reexecutar validacoes com `npm` direto no `workdir` alvo (`apps/web`) sem wrapper de caminho quoted.
CONTEXTO: Sessao 2026-02-24; Windows + PowerShell.

# ID: ERR-0020: Falha de patch por mismatch em `saveError`
SINTOMA: "Patch do `AdminSectionTogglesView` falhou em uma linha por mismatch (`saveError`)".
CAUSA_RAIZ: Drift de contexto no arquivo (linha alvo divergente do hunk aplicado).
ACAO: Reaplicar patch em blocos menores/mais localizados para evitar conflito de contexto.
CONTEXTO: Sessao 2026-02-24; `apps/web/src/modules/admin-section-toggles/components/AdminSectionTogglesView.tsx`.

# ID: ERR-0021: `spawn EPERM` do esbuild em log de fluxo da API
SINTOMA: `apps/api/tmp-flow-err.log` registrou `spawn EPERM` do `esbuild`.
CAUSA_RAIZ: Erro de execucao no ambiente (permissao/lock de processo/arquivo), sem evidencia de bug funcional direto na aplicacao.
ACAO: Tratar como incidente operacional; repetir em terminal limpo e validar bloqueios de processo/antivirus.
CONTEXTO: Sessao 2026-02-16; API Node/TypeScript em Windows.

# ID: ERR-0022: Falha de validacao por script `lint` inexistente no backend
SINTOMA: Tentativa de `apps/api: npm run lint` resultou em script inexistente.
CAUSA_RAIZ: `apps/api/package.json` nao define script `lint`.
ACAO: Validacao tecnica seguiu por `npm run build` e `npm test`; pendencia de padronizacao de script de lint no backend.
CONTEXTO: Sessoes de fevereiro/2026 no workspace JLR; Node 20.19.5, TypeScript 5.9.x.

# ID: ERR-0023: Caminho incorreto para `package.json` na raiz do monorepo
SINTOMA: `Get-Content package.json` retornou "Cannot find path 'C:\laragon\www\JLR_AI_Studio\package.json' because it does not exist".
CAUSA_RAIZ: Repositorio em formato monorepo sem `package.json` na raiz; manifests estao em `apps/api` e `apps/web`.
ACAO: Ajustar descoberta para `apps/api/package.json` e `apps/web/package.json`.
CONTEXTO: Sessao 2026-03-02; PowerShell no workspace JLR.

# ID: ERR-0024: Varredura global de sessoes excedeu limite de execucao
SINTOMA: Comando de busca em `C:\Users\Jeiel\.codex\sessions\2026` terminou com `command timed out after 120053 milliseconds`.
CAUSA_RAIZ: Escopo de busca amplo (multiplos arquivos grandes `.jsonl`) com padrao extenso em uma unica chamada.
ACAO: Repartir consulta por subconjuntos (datas/padroes), limitar retorno e iterar por etapas.
CONTEXTO: Sessao 2026-03-02; PowerShell/Codex com timeout por comando.

# ID: ERR-0025: Modal da galeria exibia imagem estourada e sem fluxo seguro de salvamento
SINTOMA: Ao abrir o editor da imagem por slot, a visualizacao ficava grande demais/cortada e o usuario fechava o modal sem acao explicita de salvar.
CAUSA_RAIZ: Preview com dimensionamento inadequado para o container e UX do modal sem CTA local de persistencia.
ACAO: Em `apps/web/src/modules/admin-media-gallery/components/AdminMediaGalleryView.tsx`, o preview foi padronizado em box fixo medio com imagem contida (`object-contain`, `max-w/max-h`) e foi adicionado fluxo explicito `Salvar e fechar` + confirmacao em `Fechar sem salvar`.
CONTEXTO: Sessao 2026-03-03; React + Vite no modulo `admin-media-gallery`; validacao manual do usuario concluida.

# ID: ERR-0026: Build Docker falhou — prop `title` obsoleto em AssinaturasContent
SINTOMA: `docker compose up -d --build api web` falhou com erro TypeScript: `Type '{ title: string; }' is not assignable to type 'IntrinsicAttributes'. Property 'title' does not exist on type 'IntrinsicAttributes'` em `AssinaturasContent.tsx(17,53)`.
CAUSA_RAIZ: `AssinaturasContent.tsx` passava `title="Faça sua Assinatura e Economize!"` para `<HomeMembershipSection>`, que antes aceitava esse prop. Apos a migracao do PLAN-0012, o componente passou a ler o titulo via `usePageText("home.membership.title")` e nao aceita mais props externos — a interface ficou incompativel.
ACAO: Removido o prop `title` da linha 17 de `apps/web/src/components/pages/AssinaturasContent.tsx`. O titulo agora vem do catalogo de textos (editavel via Admin > Textos).
CONTEXTO: Sessao 2026-06-12; PLAN-0012 migracao de componentes; Docker build no host Linux.

# ID: ERR-0027: Seed falhou no container de producao — `Cannot find module '../src/utils/logger'`
SINTOMA: `docker compose exec api npx tsx prisma/seed.ts` retornou `Error: Cannot find module '../src/utils/logger'` dentro do container da API.
CAUSA_RAIZ: O container de producao contem apenas `dist/` (codigo compilado) — o diretorio `src/` nao e copiado para a imagem final. O `tsx` tentou resolver `../src/utils/logger` em tempo de execucao, mas o arquivo nao existe no container.
ACAO: Usar o seed ja compilado pelo `seed:build` (esbuild gera `dist/seed.js` com todas as dependencias internas em bundle). Comando correto: `docker compose exec api node dist/seed.js`.
CONTEXTO: Sessao 2026-06-12; container Docker producao (node:20-slim, apenas dist/); seed rodado apos PLAN-0012.

# ID: ERR-0028: Section Toggles "acesso negado" — diagnóstico inicial errado, causa real: email hardcoded na API
SINTOMA: Admin > Seções SPA retornava "acesso negado" para o usuário MASTER (admin@jlrbeauty.com). Diagnóstico inicial errado: achamos que era `canEdit` MASTER-only no frontend. Fix provisório (ADMIN ou MASTER) revertido.
CAUSA_RAIZ_REAL: `canEditSectionToggles()` em `apps/api/src/routes/admin.ts` linha 95 verificava `user.email === "jeiel.borner@gmail.com"` (email pessoal hardcoded). O email do MASTER do sistema é `admin@jlrbeauty.com`, então a verificação nunca passava.
CAUSA_SECUNDARIA: O usuário acidentalmente trocou sua própria role de MASTER para ADMIN pela UI de Pessoas. A API bloqueia corretamente escalada para MASTER se o token atual não for MASTER (lines 79/128/183 de users.ts), criando deadlock. Role restaurada diretamente no banco via psql.
ACAO: (1) `canEditSectionToggles` reescrito para verificar `user.role === "MASTER"` em vez de email. (2) Role de `admin@jlrbeauty.com` restaurada via `UPDATE "User" SET role = 'MASTER' WHERE email = 'admin@jlrbeauty.com'`. (3) `AdminContent.tsx` e `AdminSectionTogglesView.tsx` revertidos para lógica MASTER-only original.
CONTEXTO: Sessao 2026-06-12; `apps/api/src/routes/admin.ts` linha 89–96; `apps/web/src/modules/admin-section-toggles/components/AdminSectionTogglesView.tsx`.

# ID: ERR-0029: nginx não subia após reboot do computador ##bug
SINTOMA: Após reinicialização do SO, `docker compose ps` mostrava apenas 3 containers (postgres, api, web). nginx ausente — não aparecia nem em estado de erro. Site carregava (web container servindo SPA diretamente via porta 80 interna) mas login falhava porque não havia proxy para a API.
CAUSA_RAIZ: `docker-compose.yml` definia `nginx.depends_on.api.condition: service_healthy`. O Compose CLI respeita `depends_on` durante `docker compose up`, mas o Docker daemon — responsável pelo restart automático com política `restart: unless-stopped` — ignora completamente `depends_on`. No reboot, nginx iniciava antes da API estar healthy, falhava, entrava em backoff exponencial (1s → 2s → 4s → ... → 30s) e após 34 min estava em pausa longa, invisível no `docker compose ps`.
ACAO: Alterado `condition: service_healthy` → `condition: service_started` para a dependência `api` do serviço `nginx` em `docker-compose.yml`. Nginx passa a subir junto com os demais containers e serve `502` por poucos segundos enquanto a API aquece — comportamento correto.
CONTEXTO: Sessao 2026-06-13; `docker-compose.yml` serviço nginx; Linux/Docker daemon restart policy; JLR_Beauty produção Docker.

# ID: ERR-0032: Novas seções e toggles não apareciam — `readSectionTogglesFromSettings` não mesclava com defaults ##bug
SINTOMA: Após rebuild com PLAN-0014, as seções `mission` (todas as páginas) e `about` (franquias) não apareciam no site público nem como switches no Admin > Seções Públicas. A imagem do slot apareceu corretamente.
CAUSA_RAIZ: `readSectionTogglesFromSettings` em `apps/api/src/routes/admin.ts` retornava o snapshot do banco diretamente via `cloneSectionToggleMap(parsed.data)` sem mesclar com `DEFAULT_PUBLIC_SECTION_TOGGLES`. O banco continha um snapshot antigo (salvo antes do PLAN-0014) sem as chaves `mission` e `franquias.about`. Como o snapshot era válido, o fallback nunca era acionado — as novas chaves simplesmente não existiam na resposta da API.
ACAO: Adicionada função `mergeWithDefaultSectionToggles(stored)` que parte de um clone dos defaults e sobrepõe os valores armazenados. `readSectionTogglesFromSettings` agora retorna `mergeWithDefaultSectionToggles(parsed.data)`. Isso garante que novas seções adicionadas aos defaults sempre apareçam, mesmo quando o banco tem um snapshot anterior. O comportamento é idêntico ao `normalizeSnapshot` já existente no frontend (`sectionToggles.runtime.ts`).
CONTEXTO: Sessão 2026-06-13; PLAN-0014; `apps/api/src/routes/admin.ts`; padrão — ao adicionar novas seções aos defaults, o banco nunca tem as chaves novas até o admin salvar manualmente.

# ID: ERR-0031: Build Docker falhou — `mission_center_img_01` ausente do catálogo de media slots do frontend ##bug
SINTOMA: `docker compose up -d --build` falhou com erro TypeScript no build do `web`: `Argument of type '"mission_center_img_01"' is not assignable to parameter of type '"home_hero_bg_01" | ... | "checkout_whatsapp_icon_01"'` em `MissionSection.tsx:7`.
CAUSA_RAIZ: O projeto mantém dois catálogos de media slots paralelos e independentes: `apps/api/src/modules/mediaSlots/service.ts` (backend, fonte de verdade da API) e `apps/web/src/modules/public-site/mediaSlots.ts` (frontend, lista `as const` que gera o tipo `MediaSlotId`). O hook `useMediaSlot(id)` é tipado contra o catálogo do frontend. Ao adicionar `mission_center_img_01` apenas no backend, o TypeScript do frontend rejeitou a chave como inválida.
ACAO: Adicionado `mission_center_img_01` em `apps/web/src/modules/public-site/mediaSlots.ts` — entrada idêntica à do backend (`page: "global"`, `section: "mission"`, `fallbackUrl: "/images/about_img1.webp"`). Build passou sem erros após a correção.
CONTEXTO: Sessão 2026-06-13; PLAN-0014; padrão arquitetural — qualquer novo slot de mídia precisa ser adicionado nos **dois** arquivos simultaneamente.

# ID: ERR-0033: nginx sem config após ligar o computador — contingência de drive externo
SINTOMA: Containers todos UP (`docker compose ps` mostra 4 serviços healthy), mas `http://localhost` retorna `ERR_SOCKET_NOT_CONNECTED` no browser. `curl http://localhost` conecta mas recebe "Conexão fechada pela outra ponta" imediatamente. `docker exec nginx ls /etc/nginx/conf.d/` retorna vazio.
CAUSA_RAIZ: O projeto reside em `/media/jeiel/A8FEADE5FEADABCE6/` — partição Linux criada dentro de um HD secundário compartilhado com Windows (dual-boot). O GNOME/udisks2 só monta essa partição quando o usuário clica explicitamente no gerenciador de arquivos. O Docker daemon sobe cedo no boot com `restart: unless-stopped` e recria os containers antes da partição estar montada. O bind-mount `./nginx/ → /etc/nginx/conf.d/` captura o diretório vazio (caminho inexistente no momento do start). Após a montagem manual do drive, a partição fica acessível no host mas o container já tem o bind-mount capturado apontando para nada. Nginx sobe sem nenhum `server {}` definido — aceita TCP mas fecha imediatamente.
ACAO: Não é um bug — é contingência do setup. Procedimento após ligar o computador: (1) montar o drive pelo gerenciador de arquivos; (2) executar `./scripts/fix-nginx.sh` (ou `docker compose up -d --force-recreate nginx`). Script de conveniência criado em `scripts/fix-nginx.sh`.
CONTEXTO: Sessão 2026-06-14; setup dual-boot HD compartilhado Windows/Linux; não requer alteração de código.

# ID: ERR-0034: Chaves de page text com prefixo incorreto em 3 seções — textos não carregavam ##bug
SINTOMA: Textos das seções FranquiasEtapasAbertura, FranquiasPerfilFranqueado e FranquiasSuporteFranqueadora retornavam string vazia ou fallback na tela pública após PLAN-0015.
CAUSA_RAIZ: Os 3 componentes foram codificados com prefixos longos nas chaves de page text (`etapas_abertura.*`, `perfil_franqueado.*`, `suporte_franqueadora.*`) que não correspondiam às chaves registradas em `catalog.ts` — que usavam os prefixos curtos `etapas.*`, `perfil.*`, `suporte.*`. `usePageText()` retorna a própria chave como fallback quando não encontra match, exibindo o literal da chave em vez do texto.
ACAO: Renomeadas as chaves nos 3 componentes para corresponder exatamente ao catálogo: `FranquiasEtapasAberturaSection.tsx` (`etapas_abertura` → `etapas`), `FranquiasPerfilFranqueadoSection.tsx` (`perfil_franqueado` → `perfil`), `FranquiasSuporteFranqueadoraSection.tsx` (`suporte_franqueadora` → `suporte`).
CONTEXTO: Sessão 2026-06-16; pós-PLAN-0015; `apps/web/src/modules/public-site/sections/`; padrão — ao criar seções novas, as chaves do componente devem ser copiadas literalmente do `catalog.ts`.

# ID: ERR-0035: Seções 2-col sem max-width — conteúdo esticado em monitores largos ##bug
SINTOMA: Em viewports > 1400px, as seções FounderSection, ExpansaoSection, MarketingCrmSection, PerfilFranqueadoSection e SuporteFranqueadoraSection apresentavam o grid de 2 colunas expandido indefinidamente até a borda da tela, gerando blocos de texto e imagem excessivamente largos.
CAUSA_RAIZ: Os containers internos das seções (`<div className="grid grid-cols-1 lg:grid-cols-2">`) eram filhos diretos de `<section className="w-full">` sem nenhum `max-w` limitador. O padrão do projeto é envolver o grid em `mx-auto max-w-[1200px]`, como já era feito em outras seções da mesma página.
ACAO: Adicionado `mx-auto max-w-[1200px]` ao div de grid nas 5 seções afetadas. ExpansaoSection também tinha cor de fundo incorreta (`bg-beige` — classe inexistente no Tailwind config do projeto) corrigida para `bg-gold-light`; gradiente de overlay ajustado de `to-beige/30` para `to-gold-light/30`.
CONTEXTO: Sessão 2026-06-16; pós-PLAN-0015; `apps/web/src/modules/public-site/sections/`; padrão — novas seções de largura total com grid interno devem sempre usar `mx-auto max-w-[1200px]` no container do grid.

# ID: ERR-0036: Ícones ✦ decorativos causando desalinhamento de baseline em títulos ##bug
SINTOMA: Títulos das seções BenefitsSection, GestaoAppSection e PerfilFranqueadoSection exibiam desalinhamento visual — o `<h2>` ficava fora de eixo devido à diferença de tamanho de fonte entre o ícone e o texto do título.
CAUSA_RAIZ: `<span>✦</span>` inseridos como irmãos inline de `<h2>` em contexto flex não têm alinhamento de baseline consistente com text-4xl/5xl. Em mobile, o ícone occupava espaço fora da caixa do título quebrando o layout do cabeçalho.
ACAO: Removidos os spans decorativos ✦ de BenefitsSection (2 flanqueando o h2), GestaoAppSection (após o título inline) e PerfilFranqueadoSection (antes do h2 junto com container flex).
CONTEXTO: Sessão 2026-06-16; pós-PLAN-0015; `apps/web/src/modules/public-site/sections/`.

# ID: ERR-0037: FluxoCaixaSection — layout decorativo sem imagens + 3 media slots ausentes do catálogo ##bug
SINTOMA: A seção FluxoCaixaSection exibia layout 2-col com stripe teal à direita contendo apenas ícones ✦ decorativos, sem imagem real. O conteúdo visual da seção era insuficiente para comunicar as features. Além disso, os 3 media slots de imagem por feature não existiam no catálogo (api nem web).
CAUSA_RAIZ: O PLAN-0015 implementou o layout inicial com stripe decorativa como placeholder visual, sem adicionar media slots de imagem ao catálogo. A seção foi entregue sem os slots `franquias_fluxo_caixa_feature_img_0*` em `service.ts` (api) nem em `mediaSlots.ts` (web).
ACAO: Layout substituído por grid 3-col de cards (imagem + título + descrição) com `h-[200px]` e `object-cover` por imagem. Adicionados 3 media slots (`franquias_fluxo_caixa_feature_img_01/02/03`) em `apps/api/src/modules/mediaSlots/service.ts` e em `apps/web/src/modules/public-site/mediaSlots.ts` — padrão ERR-0031 aplicado: slots devem ser adicionados nos dois arquivos simultaneamente.
CONTEXTO: Sessão 2026-06-16; pós-PLAN-0015; `FranquiasFluxoCaixaSection.tsx`, `service.ts`, `mediaSlots.ts`.

# ID: ERR-0038: EtapasAberturaSection — snake layout desproporcional em viewports médias ##bug
SINTOMA: Em telas entre 768px e 1100px, o snake de 10 passos extrapolava o container — bolhas numéricas (w-14 h-14) demasiado grandes para o espaço disponível, empurrando o texto dos passos para fora ou quebrando a linha de conexão entre os steps.
CAUSA_RAIZ: O PLAN-0015 dimensionou o container com `max-w-[1100px]` e bolhas `w-14 h-14` para um snake de 4 itens por linha. Em viewports médias, cada bolha de 56px + gap + texto `max-w-[120px]` não cabia em 4 colunas sem overflow.
ACAO: Bolhas reduzidas para `w-10 h-10` (40px), fonte interna de `text-lg` → `text-sm`; `max-w-[120px]` do texto → `max-w-[100px]`; container reduzido para `max-w-[620px]` — snake fica em layout mais compacto mas com proporções corretas. Adicionados `min-w-0` nas células e `Fragment` substituindo o wrapper extra para evitar overflow implícito.
CONTEXTO: Sessão 2026-06-16; pós-PLAN-0015; `apps/web/src/modules/public-site/sections/FranquiasEtapasAberturaSection.tsx`.

# ID: ERR-0039: AdminMediaGalleryView — Masonry Grid com alturas irregulares entre cards ##bug
SINTOMA: Na galeria Admin > Mídia, os cards de imagem exibiam alturas completamente diferentes entre si — fotos em portrait dominavam colunas inteiras enquanto imagens em landscape ficavam comprimidas, criando layout visualmente inconsistente.
CAUSA_RAIZ: O Masonry Grid de 4 colunas (PLAN-0012) distribuía slots entre colunas via `i % numCols` e renderizava imagens com `h-auto` respeitando a proporção original. Slots de franquias adicionados no PLAN-0015 têm proporções heterogêneas (ícones quadrados + fotos landscape + fotos portrait).
ACAO: Layout Masonry substituído por grid flat `grid-cols-2 md:grid-cols-4` com cada card em `div.h-[180px]` + `img.object-cover` — altura uniforme em todos os slots independente da proporção original. Lógica de colunização (`columns[]`, `i % numCols`) removida.
CONTEXTO: Sessão 2026-06-16; pós-PLAN-0015; `apps/web/src/modules/admin-media-gallery/components/AdminMediaGalleryView.tsx`.

# ID: ERR-0030: 502 Bad Gateway + tela branca após `docker compose up --build` ##bug
SINTOMA: Após rebuild com `docker compose up -d --build`, site retornava `502 Bad Gateway`. Após restart manual do nginx, site carregava mas ficava tela branca — JS do bundle era servido com `Content-Type: text/html` e tamanho 533 bytes (= index.html gzipado) em vez de 955 KB.
CAUSA_RAIZ: Dois problemas encadeados:
  (1) Bind mount de arquivo único (`./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro`): ao editar o arquivo no host, ferramentas de edição criam novo inode (write-to-temp + rename). Docker bind de arquivo único aponta para o inode original — que foi deletado. Container vê "No such file or directory" para o arquivo montado, e nginx cai para config padrão sem nenhum virtual host configurado → 502.
  (2) `proxy_pass http://$upstream_web/` com variável: no nginx, quando `proxy_pass` usa variável E inclui URI (mesmo apenas `/`), o URI do proxy_pass SUBSTITUI a URI completa da request em vez de apenas substituir o prefixo da location. Resultado: toda request (`GET /assets/index.js`, `GET /api/...`) era encaminhada ao upstream como `GET /`.
ACAO: (1) Volume alterado de bind de arquivo para bind de diretório: `./nginx/:/etc/nginx/conf.d/:ro` — diretório bind não perde referência ao editar arquivos internos. (2) Removida a `/` final de todos os `proxy_pass http://$var` no `nginx/nginx.conf` — sem URI no proxy_pass, nginx repassa a URI original intacta ao upstream.
CONTEXTO: Sessao 2026-06-13; `docker-compose.yml` volumes nginx; `nginx/nginx.conf` proxy_pass; comportamento nginx com variáveis documentado em http://nginx.org/en/docs/http/ngx_http_proxy_module.html.

# ID: ERR-0055: ProfessionalsListView — datas Início/Fim exibindo o dia anterior (fuso horário) ##bug
SINTOMA: Na tela nativa de Profissionais (Admin V2, `PLAN-0026` Onda 13), as colunas "Início"/"Fim" da tabela exibiam um dia antes do valor real salvo no banco (ex.: `startedAt` = `2026-08-13T00:00:00.000Z` no Postgres, mas a tabela mostrava "12/08/2026").
CAUSA_RAIZ: `startedAt`/`endedAt` (`Professional`, `schema.prisma`) são normalizados no backend como data pura à meia-noite UTC (`parseIsoDateStart`, `apps/api/src/lib/routeHelpers.ts`). `formatDateOnly` (novo, `ProfessionalsListView.tsx`) formatava com `new Date(value).toLocaleDateString("pt-BR")` sem fixar o fuso — o método converte pro fuso horário local do runtime (container/servidor) antes de formatar; em qualquer fuso negativo (ex.: America/Sao_Paulo, UTC-3), meia-noite UTC vira ~21h do dia anterior, e a data exibida "vira" o dia anterior.
ACAO: `formatDateOnly` corrigido para `toLocaleDateString("pt-BR", { timeZone: "UTC" })`, forçando a formatação a usar os componentes UTC da data (que já são os corretos, dado que o valor já é meia-noite UTC por construção). O modal de edição (`toDateInputValue`, `ProfessionalFormModal.tsx`) já usava `toISOString().slice(0, 10)` — sempre UTC por definição — e não tinha o bug; só a exibição na tabela precisou do fix.
CONTEXTO: Sessão 2026-08-16; `PLAN-0026` Onda 13 (Profissionais); achado na validação visual real (Chrome), não no E2E via curl (o payload da API já estava correto — o bug era só de exibição no frontend); `apps/web/src/admin-v2/cadastros/professionals/ProfessionalsListView.tsx`. Nota de processo: qualquer formatação de data-pura-armazenada-como-UTC-meia-noite deve sempre fixar `timeZone: "UTC"` — o mesmo risco existe em qualquer tela futura que exiba `startedAt`/`endedAt`-like fields.

# ID: ERR-0056: Cadastro > Clientes sempre vazio — Customer nunca era materializado ##bug
SINTOMA: `PLAN-0027` (reportado pelo usuário) — a tela nativa "Cadastro > Clientes" (`PLAN-0026` Onda 12) não mostrava nenhum cliente, enquanto "Panorama > Clientes" mostrava 17.
CAUSA_RAIZ: As duas telas leem fontes de dados diferentes. `GET /api/customers` (`apps/api/src/routes/schedule.ts:549`) lê a tabela `Customer` do Prisma, que nunca era escrita por nenhum fluxo operacional — só por `POST /customers` da própria tela nativa, nunca usada em produção. `GET /api/admin-v2/customers` (Panorama) não lê `Customer`; deriva os "17" agregando `Order`+`Appointment`+`Subscription` por identidade (`apps/api/src/modules/intelligence/customers/service.ts:69-90`).
ACAO: Criado `apps/api/src/lib/customerSync.ts` (`syncCustomerFromContact`, upsert por telefone normalizado — chave natural de `Customer.phone` `@unique`). Chamado em 2 pontos únicos de escrita: `markOrderAsPaid` (`apps/api/src/lib/fulfillmentUtils.ts`, cobre Stripe webhook + venda manual balcão, único choke-point de "pedido virou PAGO") e na criação de agendamento (`apps/api/src/lib/appointmentAvailability.ts`, único ponto de `appointment.create` no repo). Best-effort (try/catch + `logger.warn`, nunca derruba o fluxo principal). Backfill único criado em `apps/api/scripts/backfillCustomersFromContacts.ts` (`npm run backfill:customers`, idempotente) para materializar o histórico já existente.
CONTEXTO: Sessão 2026-08-17; `PLAN-0027` Item 1; validado E2E real (venda manual `markAsPaid` + agendamento novo, ambos materializaram `Customer` na hora; backfill rodado em produção-dev, 8 clientes reais criados) e visual real (Chrome, tela lista os 8). Achado colateral não corrigido, ver `ERR-0058`: o total do backfill (8, por telefone) é menor que os "17" do Panorama porque o Panorama tem um bug próprio de contagem duplicada.

# ID: ERR-0057: Sessão do Admin expira em 15min sem renovação — refresh token nunca era usado pelo frontend ##bug
SINTOMA: `PLAN-0027` (reportado pelo usuário) — usuário trocava de aba por alguns minutos (ex.: digitar em outro app) e ao voltar já estava deslogado, precisando logar de novo.
CAUSA_RAIZ: `getToken()` (`apps/web/src/lib/auth.ts:92-100`) decodifica o `exp` do JWT no cliente e, se expirado (skew de só 30s), chama `clearAuth()` e devolve `null` na hora — sem nunca tentar renovar. O access token dura `JWT_EXPIRES_IN=15m`. O backend já tinha um fluxo de refresh completo e funcional (cookie httpOnly `jlr_rt`, 7 dias, `POST /api/auth/refresh`, `apps/api/src/routes/auth.ts:259-299`), mas nenhum lugar do frontend chamava esse endpoint.
ACAO: Adicionado a `apps/web/src/lib/auth.ts`: `refreshAccessToken()` (chama `/api/auth/refresh` com `credentials: "include"`) e `initSessionKeepAlive()` (interval de 5min + listener de `visibilitychange`, disparado via o `AUTH_STATE_EVENT` já existente). Chamado uma vez no bootstrap (`apps/web/src/main.tsx`). `apps/web/src/app/RequireAdmin.tsx` também passou a tentar um refresh antes de decidir "sessão negada", fechando o caso de reload após ausência longa (aba suspensa/fechada).
CONTEXTO: Sessão 2026-08-17; `PLAN-0027` Item 2; validado via `POST /api/auth/refresh` real com cookie de sessão (renovou o access token corretamente) + `tsc -b` limpo + confirmação de que a string `auth/refresh` está no bundle de produção servido.

# ID: ERR-0058: Panorama > Clientes conta o mesmo cliente físico 2x (chave de identidade inconsistente) ##bug
SINTOMA: Achado durante a validação do `ERR-0056` (não reportado pelo usuário) — o backfill de `Customer` por telefone materializou só 8 clientes reais, menos da metade dos "17" que o Panorama > Clientes mostra, para os mesmos dados.
CAUSA_RAIZ: `identityKey` (`apps/api/src/modules/intelligence/customers/service.ts:15-16`) usa prioridade `email > telefone > nome`. Pedidos sempre têm `customerEmail` preenchido (agrupam por e-mail). Agendamentos não têm e-mail direto — só `appointment.client?.email`, e `Appointment.clientId` está `NULL` para todos os registros atuais (confirmado via SQL) — então agrupam pelo fallback, telefone. Resultado: o mesmo cliente físico (1 telefone) gera 2 entradas na Map — uma pelo pedido (chave = e-mail), outra pelo agendamento (chave = telefone) — porque as duas fontes nunca convergem pra mesma chave quando o agendamento não tem usuário vinculado. 8 clientes reais × ~2 entradas + 1 assinatura inadimplente = 17.
ACAO: ✅ CORRIGIDO 2026-08-17 (mesma sessão, após decisão explícita do usuário — "clientes duplicados, junte num único"). `identityKey` trocada para `telefone > email > nome` (telefone normalizado só-dígitos, `apps/api/src/modules/intelligence/customers/service.ts`). Validado via API real: `/api/admin-v2/customers` foi de 17 para 9 (8 clientes reais únicos + 1 assinatura inadimplente sem pedido/agendamento correspondente) e via visual real (Chrome, NOVOS 8 + EM RISCO 1 = 9). `unit-health/service.ts`/`dashboardSalesInsights.ts` usam a mesma convenção de forma independente e **não foram alterados** — mesmo risco pode existir lá, fora do escopo deste fix.
CONTEXTO: Sessão 2026-08-17; `PLAN-0027` Item 4; `apps/api/src/modules/intelligence/customers/service.ts`, `apps/api/src/modules/intelligence/customers/types.ts`.

# ID: ERR-0059: Cadastro > Clientes e Cadastro > Serviços — grid mostra o ID, form de edição não ##bug
SINTOMA: `PLAN-0027` (reportado pelo usuário) — em `Cadastro > Clientes` e em `Cadastro > Serviços`, a grid mostra `CLI-{id}`/`SRV-{id}` por registro, mas o form de edição de nenhuma das duas telas expunha o ID em nenhum campo.
CAUSA_RAIZ: omissão de UI, não de dado — `CustomerFormModal.tsx`/`ServiceFormModal.tsx` nunca tiveram um campo pra mostrar `editing.id`; o valor sempre existia (`Customer.id`/`Service.id`), só não era renderizado.
ACAO: campo "ID" somente-leitura (`disabled`, `readOnly`) adicionado como primeiro campo de cada form, antes de Nome — `CLI-{id}`/`SRV-{id}` quando editando, "gerado ao salvar" quando criando. No `CustomerFormModal.tsx`, aproveitado o mesmo commit pra trocar "ID de usuário" de posição com E-mail (pedido do usuário — Item 7) e esclarecer via `title` que "ID de usuário" é um vínculo opcional com `User` (conta de login), não o ID do próprio cliente e não é preenchido automaticamente.
CONTEXTO: Sessão 2026-08-17; `PLAN-0027` Itens 5/7/9; `apps/web/src/admin-v2/cadastros/customers/components/CustomerFormModal.tsx`, `apps/web/src/admin-v2/cadastros/services/components/ServiceFormModal.tsx`.

# ID: ERR-0060: Cadastro > Serviços sem filtro de Destaque + isFeatured nunca usado (9 flip-cards da Home desconectados de Service) ##bug
SINTOMA: `PLAN-0027` (reportado pelo usuário) — usuário queria filtrar serviços por "Destaque" pra saber quais aparecem nos 9 flip-cards da Home pública, e notou que nenhum dos 75 serviços cadastrados estava marcado, mesmo a Home tendo 9 cards reais.
CAUSA_RAIZ: dupla. (1) `Service.isFeatured` e o checkbox "Destaque (flip-cards)" já existiam no form desde o `PLAN-0026`, mas `ServicesListView.tsx` nunca teve filtro por esse campo — só categoria/status. (2) Os 9 flip-cards (`HomeServicesSection.tsx`) são conteúdo 100% estático: imagem em alta definição vem de 9 media slots (`home_services_card_img_01..09`, `Cadastro > Galeria de Mídias`), textos de front/back vêm de 36 page texts (`home.services.card_N_*`, `Cadastro > Textos das Páginas`), e o botão "Agendar" usa `data-concierge-category`/`data-concierge-service` hardcoded como texto literal no JSX — nada disso referencia `Service.id`. `Service.isFeatured`/`Service.imageUrl` são campos paralelos e desconectados; nenhum serviço tinha `imageUrl` preenchido (confirmado via SQL).
ACAO: filtro "Destaque: todos/sim/não" adicionado em `ServicesListView.tsx` (mesmo padrão de categoria/status). Reconciliação de dados: os 9 `data-concierge-service` dos flip-cards foram cruzados por nome com a tabela `Service` (8 serviços reais e distintos — os cards "Pele Clínica" e "Facial Spa" apontam pro mesmo serviço, "Limpeza de Pele") e marcados `isFeatured = true` via `UPDATE` direto no Postgres (ids 16, 30, 42, 45, 48, 52, 58, 73). Religar os flip-cards pra consumir `Service` em runtime (em vez de conteúdo estático) é decisão de arquitetura em aberto, não implementada — ver `PLAN-0027` Item 10.
CONTEXTO: Sessão 2026-08-17; `PLAN-0027` Item 10; `apps/web/src/admin-v2/cadastros/services/ServicesListView.tsx`, `apps/web/src/modules/public-site/sections/HomeServicesSection.tsx`.

# ID: ERR-0061: Cadastro > Planos sem campo de imagem — imagem pública vinha de media slots genéricos, sem vínculo com o plano real ##bug
SINTOMA: `PLAN-0027` (reportado pelo usuário) — ao editar um plano, não havia campo de imagem; usuário suspeitava que a imagem exibida no site vinha "fixa da galeria".
CAUSA_RAIZ: confirmada a suspeita — `Membership` (schema) não tinha campo de imagem, e a seção pública (`AssinaturasHeroSection.tsx`) usa 3 media slots genéricos e posicionais (`assinaturas_hero_card_img_01/02/03`), sem nenhum vínculo com `Membership.id`/`name`. A seção da Home (`HomeMembershipSection.tsx`) é ainda mais estática — nomes/preços/benefícios dos 3 planos são texto hardcoded no JSX, nem chega a consumir `/api/memberships`.
ACAO: mudança aditiva — migração `20260817180000_add_membership_image` (`Membership.imageUrl String?`, nullable, sem default); `apps/api/src/routes/subscriptions.ts` aceita/retorna `imageUrl` em `POST`/`PATCH /memberships`; `PlanFormModal.tsx` ganhou campo "Imagem do plano" com upload real (mesmo padrão do `ServiceFormModal.tsx`); `PlanCard.tsx` mostra a miniatura quando existe. Religar a renderização pública pra consumir esse campo é decisão de arquitetura em aberto, não implementada — ver `PLAN-0027` Item 11.
CONTEXTO: Sessão 2026-08-17; `PLAN-0027` Item 11; migração `apps/api/prisma/migrations/20260817180000_add_membership_image/`, `apps/api/src/routes/subscriptions.ts`, `apps/web/src/admin-v2/cadastros/plans/`.

# ID: ERR-0062: Flip-cards de Serviços e cards de Planos religados ao cadastro nativo (PLAN-0028)
SINTOMA: `PLAN-0027` Itens 10/11 tinham deixado em aberto que os 9 flip-cards de Serviços (Home) e os cards de Planos (Assinaturas) não liam `Service.isFeatured`/`Membership.imageUrl` do cadastro nativo — continuavam usando media slots/page texts estáticos (Serviços) ou não tinham campo de imagem nenhum (Planos). Usuário decidiu religar os dois em vez de deixar como achado documentado.
CAUSA_RAIZ/ACHADOS: (1) Planos — achado que **corrigiu** uma afirmação errada do fechamento do `PLAN-0027`: `HomeMembershipSection.tsx` já era data-driven via `renderMembershipsFromDb()`/`loadMembershipsFromDb()` em `index.behavior.ts` (busca `/api/public/memberships` real); só faltava renderizar `imageUrl` no template, que nunca existiu ali. (2) Serviços — os 4 textos de cada flip-card (front/back label/tagline) são conteúdo de marketing genuinamente distinto do nome operacional do serviço (ex.: front "Lashes" / real `Service.name` "Extensão De Cilios"); `Service` não tinha campos equivalentes. (3) Achado técnico extra descoberto na implementação: `HomeServicesSection.tsx` passou a renderizar os 9 cards de forma assíncrona (fetch), mas o binding de clique dos botões "Agendar" (`[data-open-concierge]`) em `index.behavior.ts` era direto nos elementos (`document.querySelectorAll` executado uma vez no boot) — os botões renderizados depois do fetch nunca receberiam o listener. (4) Achado de conteúdo: 2 dos 9 cards ("Pele Clínica"/card 2 e "Facial Spa"/card 7) apontavam pro mesmo serviço real (`Limpeza de Pele`), sem condição de existir 2 conjuntos de texto distintos numa única linha de `Service`.
ACAO: **Planos** — `imageUrl` adicionado a `HomeMembershipRow`/`parsePublicMemberships`/`renderMembershipsFromDb` (`index.behavior.ts`); renderiza `<img>` só quando o plano tem imagem. **Serviços** — migração aditiva `Service.highlightLabel/highlightTagline/highlightBackLabel/highlightDescription/highlightOrder` (`20260817190000_add_service_highlight_fields`); endpoint novo `GET /public/services/featured`; `ServiceFormModal.tsx` ganhou bloco condicional "Destaque na Home"; `HomeServicesSection.tsx` reescrito pra `.map()` sobre o fetch, visual 1:1 preservado; conteúdo dos 36 textos + 9 imagens migrado via `apps/api/scripts/seedServiceHighlights.ts` (idempotente) pros 9 serviços corretos — decisão do usuário: card 7 trocado de `Limpeza de Pele` (duplicado) pra `Drenagem Linfática Facial` (id 34, serviço real de Estética Facial ainda não coberto), resolvendo a duplicidade sem perder a contagem de 9 cards. **Binding de clique** — `openConciergeFromTrigger`/listener de `[data-open-concierge]` convertido pra delegação em `document` (`target.closest(...)`), mesmo padrão já usado pra `[data-membership-join]`/`[data-checkout]` no mesmo arquivo — funciona independente de quando o botão entra no DOM.
CONTEXTO: Sessão 2026-08-17; `PLAN-0028` (desmembrado do `PLAN-0027` Itens 10/11); `apps/web/src/modules/public-site/index.behavior.ts`, `apps/web/src/modules/public-site/sections/HomeServicesSection.tsx`, `apps/api/src/routes/catalog.ts`, `apps/api/src/routes/subscriptions.ts`, `apps/api/prisma/schema.prisma`. Validado E2E real (`GET /public/services/featured` retornando os 9 serviços corretos e ordenados; `PATCH /memberships/:id` com `imageUrl` round-trip) e visual real (Chrome: 9 flip-cards com labels/imagens corretos, flip + botão "Agendar" abrindo o concierge com o serviço certo pré-preenchido; card de plano exibindo imagem quando cadastrada). Chaves antigas em `pageTexts/catalog.ts`/`mediaSlots/service.ts` pra esses 9 slots ficam órfãs (nenhuma tela mais usa) — decisão de limpeza adiada, não urgente.

# ID: ERR-0063: Crescimento > Franquias — motivo da troca de etapa era obrigatório mas nunca aparecia no card ##bug
SINTOMA: Usuário reportou (2026-08-17) — desde o `PLAN-0025` item 3, o motivo/evento da mudança de etapa é obrigatório (modal pede antes de confirmar), mas depois de salvo o motivo não aparecia em lugar nenhum na tela — só ficava gravado no histórico, invisível pro usuário.
CAUSA_RAIZ: `getFranchisePipeline()` (`apps/api/src/modules/intelligence/franchise-pipeline/service.ts`) já buscava `FranchiseLeadStageHistory` pra calcular métricas de tempo médio/gargalo, mas o `select` não incluía a coluna `reason`, e o tipo de saída `PipelineLead` (`types.ts`) nunca teve esse campo — o dado existia no banco desde o `PLAN-0025`, só nunca era lido de volta pro frontend.
ACAO: `reason: true` adicionado ao `select` do histórico; calculado o registro mais recente por lead (`latestReasonByLead`, maior `changedAt`) e anexado a `LeadSnapshot`/`PipelineLead` (backend + frontend `types.ts`), passado adiante em `metrics.ts`. `LeadCard.tsx` (Admin V2) ganhou uma linha "Motivo" (só quando existe) com tooltip nativo (`title`) — passa o mouse e o texto completo aparece, sem componente de tooltip customizado novo.
CONTEXTO: Sessão 2026-08-17; `apps/api/src/modules/intelligence/franchise-pipeline/service.ts`, `types.ts`, `metrics.ts`; `apps/web/src/admin-v2/growth/franchises/types.ts`, `components/LeadCard.tsx`. Validado E2E real (`PATCH /admin-v2/growth/franchises/:id/stage` com `reason`, `GET` devolvendo o motivo certo) e visual real (Chrome, tooltip "Motivo" confirmado via árvore de acessibilidade — tooltip nativo não aparece em screenshot headless, mas o atributo `title` foi confirmado com o texto exato). Mudança de teste (lead 7, movido pra REUNIAO e revertido pra QUALIFICADO) desfeita ao final; achado curioso: vários leads reais já tinham motivo gravado de uso anterior, confirmando que o dado nunca foi perdido, só nunca exibido.

# ID: ERR-0064: Drag-and-drop nos kanban do Admin V2 (Operação + Franquias, PLAN-0029)
SINTOMA: Usuário pediu (2026-08-17) mover pedidos/leads arrastando o card entre colunas, em vez de usar controles explícitos, pensando numa rotina reusável pros kanban do Admin V2.
CAUSA_RAIZ/ACHADO: os dois boards tinham mecanismos de dado bem diferentes. `Operação > Pedidos` — colunas são **calculadas** a cada load (`columnFor()`, `operational-orders/service.ts`), não um campo salvo; "Atenção" é um alerta de tempo, não uma etapa manual. `Crescimento > Franquias` — `FranchiseLead.stage` é um campo real, mas o `PLAN-0022`/RETROFIT-010b tinha decisão explícita registrada de "usuário nunca arrasta" (consistência de interação com o Mapa da Rede). Perguntas feitas ao usuário antes de codar (ver `DECISION-015`): comportamento da coluna "Atenção"/"Entraram" (fixas, não-arrastáveis); valor de `fulfillmentStatus` gravado ao soltar em "Prontos" (`DESPACHADO`) e "Em Preparação" (`SEPARANDO`); confirmação explícita da reversão da regra "nunca arrasta" em Franquias.
ACAO: Componente reusável novo `apps/web/src/admin-v2/shell/kanban/KanbanDndBoard.tsx` (`@dnd-kit/core`, dependência nova aprovada explicitamente pelo usuário — `react-beautiful-dnd` está arquivada/incompatível com React 19, API nativa exigiria reconstruir acessibilidade/touch do zero). `OrdersBoardView.tsx` — drag entre `emPreparacao`↔`prontos` (colunas fixas: `entraram`/`atencao`), reusa `PATCH /orders/:id/fulfillment` (endpoint legado já existente, sem mudança). `PipelineBoardView.tsx`/`LeadCard.tsx` — `<select>` removido, card inteiro vira arrastável entre qualquer par de etapas (movimento livre, regra de negócio inalterada), soltar abre o mesmo modal de motivo obrigatório de antes (`StageChangeReasonModal`) antes de confirmar; cancelar reverte a UI sozinho (board sempre renderiza a partir do dado real, nunca estado otimista). `DECISION-015` registrada, substituindo formalmente a regra de RETROFIT-010b.
CONTEXTO: Sessão 2026-08-17; `apps/web/src/admin-v2/shell/kanban/KanbanDndBoard.tsx` (novo), `operations/orders/OrdersBoardView.tsx`, `growth/franchises/PipelineBoardView.tsx`, `growth/franchises/components/LeadCard.tsx`, `shared/api.ts` (`updateOrderFulfillmentStatus` novo). Validado E2E real via simulação de eventos de ponteiro nativos (`PointerEvent` com sequência pointerdown→pointermove×N→pointerup e delays reais entre eles — `left_click_drag`/drags instantâneos não davam tempo do React processar os estados intermediários do dnd-kit, mas o mecanismo real funciona igual com interação humana de verdade) contra o Postgres real: pedido movido pra "Em Preparação" gravou `SEPARANDO` e foi corretamente reclassificado pra "Atenção" pelo classificador de idade (comportamento correto, não bug — "Atenção tem prioridade sobre o estágio natural", regra já existente); lead de franquia arrastado abriu o modal de motivo, confirmado, gravou `stage`+`reason` reais, revertido ao final. Achado colateral sem risco: o `PATCH /orders/:id/fulfillment` (endpoint legado, não alterado) também atualiza `Order.status` como efeito colateral ao setar `fulfillmentStatus=ENTREGUE` — pedido de teste (`seedAdminV2TestData`, sintético) ficou com `status=ENTREGUE` em vez do `PAGO` original após o teste; sem impacto real (dado sintético, não cliente real), documentado aqui pra não confundir numa averiguação futura.

# ID: ERR-0065: Crescimento > Franquias — tooltip nativo do Motivo lia como "?" em vez do texto ##bug
SINTOMA: Usuário reportou (2026-08-18) — passar o mouse em cima de "Motivo" no card do Kanban de Franquias mostrava "?" em vez do motivo cadastrado; quando não havia motivo, a linha simplesmente não aparecia (sem indicar "N/A").
CAUSA_RAIZ: `ERR-0063` tinha implementado o tooltip via atributo `title` nativo do navegador + classe `cursor-help`. O `cursor: help` troca o cursor do mouse por um ícone de seta com "?" — isso aparece instantaneamente ao passar o mouse; o balão nativo do `title` com o texto real só surge depois de um delay (~1s) que passa despercebido/é fácil de não esperar. O usuário via o "?" do cursor e nunca chegava a ver o balão nativo.
ACAO: `LeadCard.tsx` — `title`/`cursor-help` nativo trocado por tooltip próprio (`group`/`group-hover`, `apps/web/src/admin-v2/growth/franchises/components/LeadCard.tsx`), sem delay, sempre visível ao passar o mouse. A linha "Motivo" deixou de ser condicional (`lead.reason &&`) — passou a aparecer sempre, mostrando `lead.reason ?? "N/A"` no tooltip.
CONTEXTO: Sessão 2026-08-18; `apps/web/src/admin-v2/growth/franchises/components/LeadCard.tsx`. `tsc -b`/build limpos; validado visualmente via Chrome real (hover confirmado mostrando o texto real gravado e "N/A" num lead sem motivo).

# ID: ERR-0066: Operação > Pedidos — mover fulfillmentStatus não tirava o card de "Entraram" ##bug
SINTOMA: Usuário reportou (2026-08-18) pedidos parados na coluna "Entraram" sem conseguir arrastar pra "Em Preparação". Antes de mudar o desenho do board (`PLAN-0030`), tentei a solução mais simples primeiro (destravar o drag reusando `PATCH /orders/:id/fulfillment`, gravando `SEPARANDO`) — validada ao vivo (token real, drag simulado, Postgres real), essa solução **não funcionou**.
CAUSA_RAIZ: `columnFor()` (`operational-orders/service.ts`) decide "Entraram" só por `Order.status === "PENDENTE"` — nunca olha `fulfillmentStatus`. Gravar `fulfillmentStatus = SEPARANDO` num pedido com `status` ainda `PENDENTE` não muda a coluna calculada (continua em "Entraram") e ainda deixa o dado inconsistente (`separatedAt` preenchido num pedido não confirmado como pago).
ACAO: Reformulação completa do board pro fluxo real de 5 etapas (`PLAN-0030`) — "Entraram" virou "Recebido", e a única forma de sair de lá é confirmar `status = PAGO` (endpoint `PATCH /orders/:id`, reusado), nunca só fulfillment. Ver `DECISION-016`.
CONTEXTO: Sessão 2026-08-18; achado só de investigação (não chegou a ser mergeado — revertido no mesmo teste, pedido `PV-MSRI9C27-V84H`/id 5 restaurado via SQL direto assim que confirmado o problema). Motivou o `PLAN-0030` inteiro.

# ID: ERR-0067: Sessão expirava sempre em 15min — cookie de refresh `Secure` sem HTTPS ##bug
SINTOMA: Usuário reportou (2026-08-18) — último login fazia tempo, apareceu tela de login, logou de novo, foi pro admin legado; ao abrir admin-v2, erro de "preciso me logar" em todas as telas.
CAUSA_RAIZ: O cookie de refresh (`jlr_rt`, 7 dias, `ERR-0057`) é gravado com `secure: isProduction` (`apps/api/src/routes/auth.ts`). Em produção `NODE_ENV=production`, então `secure: true` — mas a produção roda em HTTP puro, sem TLS (`SEC-30`/`PLAN-0019`, bloqueado por domínio). Um cookie `Secure` recebido por HTTP é descartado pelo navegador (nunca chega a ser armazenado) — confirmado ao vivo via `curl -sD -` mostrando `Set-Cookie: jlr_rt=...; Secure` servido em `http://localhost`. Resultado: a renovação silenciosa (`refreshAccessToken()`) nunca tinha o cookie pra enviar, sempre falhava, e toda sessão morria exatamente no TTL do access token (`JWT_EXPIRES_IN=15min`), sem exceção — em qualquer tela, admin ou admin-v2.
ACAO: Variável nova `TLS_ENABLED` (`.env`, `.env.docker.example`, `sfk.toml`) — `secure` do cookie passa a depender dela, não de `NODE_ENV`. Default `false` (comportamento real de hoje, sem TLS): cookie sem `Secure`, funciona sobre HTTP. Setar `TLS_ENABLED=true` assim que o `PLAN-0019` sair do bloqueio (domínio + TLS reais). Sem aumento de risco: a sessão inteira (incluindo o Bearer token em cada request) já trafegava sem criptografia antes disso — o contorno só restaura o comportamento de sessão longa que já era o desenho original (`ERR-0057`).
CONTEXTO: Sessão 2026-08-18; `apps/api/src/routes/auth.ts`. Validado ao vivo: `curl` confirmando `Set-Cookie` sem `Secure` após o fix; fluxo completo testado via `fetch` real no navegador (login → cookie armazenado de verdade pelo Chrome → `POST /auth/refresh` retornou 200 com token novo, usando o cookie). `tsc`/build/testes (134/134) limpos.

# ID: ERR-0068: `refreshAccessToken()` apagava um access token ainda válido quando a renovação falhava ##bug
SINTOMA: Achado durante a investigação do `ERR-0067` — mesmo com um access token recém-emitido e ainda válido por vários minutos, um hard-reload/nova navegação (`/admin`, `/admin-v2`) deixava a sessão "quebrada" quase na hora, não só depois dos 15min.
CAUSA_RAIZ: `initSessionKeepAlive()` (`apps/web/src/lib/auth.ts`) chama `refreshAccessToken()` imediatamente a cada boot do app. Se a renovação falhasse por qualquer motivo (cookie ausente — caso do `ERR-0067` — ou qualquer outro 401/erro do endpoint), o código chamava `clearAuth()`, apagando o access token do `localStorage` **mesmo que ele ainda estivesse perfeitamente válido**. Ou seja: uma falha de renovação não só deixava de renovar — ela ativamente destruía a sessão corrente.
ACAO: `refreshAccessToken()` não chama mais `clearAuth()` quando a resposta não é `ok` — só retorna `false` (falha em renovar, mas não desloga). `getToken()` já cuida de limpar sozinho quando o access token realmente expira (checagem de `exp` no próprio token); a responsabilidade de expirar a sessão fica só ali.
CONTEXTO: Sessão 2026-08-18; `apps/web/src/lib/auth.ts` (`refreshAccessToken`). Validado ao vivo junto com o `ERR-0067` — sessão sobrevive a hard-navigation logo após o login, sem "sessão expirada" prematura. `tsc`/build limpos.

# ID: ERR-0069: Bot de status do Docker reportava API/Postgres falsamente offline com sessão expirada ##bug
SINTOMA: Usuário reportou (2026-08-18) — o widget de status no admin legado mostrava API e PostgreSQL como "fora", mas `docker ps` em outro terminal mostrava todos os containers rodando.
CAUSA_RAIZ: `useDockerHealth.ts` — `GET /health/services` exige admin autenticado (`requireAdmin`). Quando a chamada falhava por qualquer motivo (401 de sessão expirada — mesma causa raiz do `ERR-0067`/`ERR-0068` — ou erro de rede), o código assumia `{ api: "offline", postgres: "offline", ... }` como fallback fixo, sem distinguir "não consegui checar" de "checei e está mesmo offline".
ACAO: Novo estado `unknown` em `ServiceStatus`, distinto de `offline`. Falha de auth/rede agora retorna todos os serviços como `unknown` ("Não verificado"), nunca mais `offline` fabricado. `anyOffline` (LED do topbar) só considera `offline` de verdade, nunca `unknown`.
CONTEXTO: Sessão 2026-08-18; `apps/web/src/modules/admin-docker-status/useDockerHealth.ts`, `DockerStatusModal.tsx`. Confirmado que o endpoint exige `requireAdmin` (`apps/api/src/app.ts`) e retorna 401 com token inválido (testado ao vivo). `tsc`/build limpos.
