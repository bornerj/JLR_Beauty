# Progress — Current Module State

> Current state of the project. Updated after each completed task.
> Complements `memory/MODIFICATION_LOG.md` (chronological history).
> This file answers the question: **"What is ready right now?"**

---

## Resume Panel

_(lido no início de cada sessão, conforme `.sfk/kernel/OPERATING_CARD.md` item 1 — atualizado a cada fechamento de sessão)_

- **Última sessão fechada:** esta (2026-08-24), auditoria PASS — ver `memory/logs/AUDIT_CHECKLIST_20260824_225807-PASS.md`. Retomou de uma pausa não formal em 2026-08-20.
- **Estado:** sessão longa com 2 blocos de trabalho, ambos `DONE`, commitados e pushados (16 commits, `origin/main` sincronizado):
  1. **`PLAN-0034`** (auditoria de textos/imagens hardcoded pós-Admin V2, `DECISION-018`) fechado formalmente — ver `memory/plans/PLAN-0034-DONE-AUDITORIA-TEXTOS-HARDCODED-POS-ADMIN-V2.md`. Achado colateral resolvido junto: `.gitignore` restaurado (`/.codex/`) + `.sfk/kernel/skills/` (197 arquivos) destrackeado do git (framework sincronizado automaticamente, não é produto do projeto).
  2. **Auditoria completa do `@code-archaeologist` sobre `admin-v2`** (116 arquivos, 4 subagentes em paralelo) — 14 achados, todos endereçados. `PLAN-0035` fechado `DONE` (12 achados médios/baixos) — ver `memory/plans/PLAN-0035-CORRECOES-AUDITORIA-ADMIN-V2.md`. Mais 13 bugs corrigidos no total (`ERR-0075` a `ERR-0088`), incluindo a expansão do erro de lint `set-state-in-effect` pra 27 arquivos do repo inteiro (não só os 11 do escopo original da auditoria) — **lint do `apps/web`: 28 → 0 erros**. `checklist.py` do kernel corrigido (`ERR-0088`, nunca tinha rodado de verdade antes). `DECISION-020` registrada (múltiplos `<h1>` nas seções públicas são intencionais, não bug).
- **next_action:** nenhuma ação pendente de aprovação — tudo commitado e pushado. Achado #14 do `code-archaeologist` (hook compartilhado `useAdminV2Fetch`, DRY dos 11 dashboards) fica **deferred** por decisão explícita do usuário, disponível se quiser retomar. `verify_all.py` (kernel) não testado nesta sessão — vale desconfiar até testar, dado que `checklist.py` tinha bugs não notados. Itens de sessões anteriores, ainda não verificados: possível contagem duplicada equivalente ao `ERR-0058` em `unit-health/service.ts`/`dashboardSalesInsights.ts`; tela nativa "Assinantes" como candidata a feature nova; `flows.spec.ts` "home cart and checkout flow" quebrado (pré-existente); duplicação do catálogo de `mediaSlots` em 3 lugares.
- **Planos antigos ainda em aberto:** `PLAN-0019` (TLS/HTTPS, bloqueado por domínio — dependência externa). `PLAN-0021` (Menu Admin) fechado **SUPERADO** em 2026-08-20.

---

## Modules

<!-- Available states: stable | in-progress | blocked | planned | deprecated -->
<!-- Update this file whenever a PLAN-XXXX is marked as DONE -->

| Module | State | Updated | Notes |
|--------|-------|---------|-------|
| Infra Docker + PostgreSQL | stable | 2026-06-11 | docker compose up funcional, healthchecks OK, migration + seed aplicados |
| API Routes (refactor) | stable | 2026-06-11 | 9 arquivos de domínio, 6 libs, index.ts 23 linhas, builds e testes passando |
| Prisma Schema | stable | 2026-06-11 | provider=postgresql, migration init única, binaryTargets para Docker Alpine |
| Section Toggles | stable | 2026-06-11 | fs.writeFileSync removido, usa apenas tabela Setting |
| Page Texts Editor | stable | 2026-06-16 | PLAN-0012 FECHADO — 129 campos, quebra de linha, histórico (versão anterior), galeria Masonry |
| Docker Status Modal | stable | 2026-06-13 | PLAN-0013 CONCLUÍDO — modal flutuante admin, auto-fecha 10s, ícone topbar com LED |
| Mission Section | stable | 2026-06-16 | PLAN-0014 CONCLUÍDO — About Franquias + MissionSection 3-col; width corrigido (max-w-[1440px]) |
| Franquias Hero Gallery | stable | 2026-06-16 | toggle hero_gallery via section toggles — oculto por padrão, reativável pelo Admin |
| Franquias Page Upgrade | stable | 2026-06-20 | PLAN-0015 DONE — 18 seções; correções pós-entrega: ERR-0034..0039 (chaves page text, max-width, ✦ removidos, FluxoCaixa layout+slots, Etapas snake, AdminGallery grid) |
| Unified Navigation Menu | stable | 2026-06-20 | PLAN-0016 DONE — nav único para Home/Assinaturas/Franquias; dropdowns Assinaturas (novo) e Franquias (7 landmarks); mobile menu unificado |
| Home About Section Gallery | stable | 2026-06-22 | Flowbite Featured Image (1 destaque + 5 miniaturas); layout top/bottom; slots img_07/08 removidos; CSS via style inline (Docker build) |
| Franquias Fine-Tuning Visual | stable | 2026-06-21 | bg alternante A/B (13 seções, altMap dinâmico); object-contain em 3 imagens; quote circle no Founder; cards modelos reordenados (ESSENCIAL→PRIME→MASTER); nomes abaixo das imagens; botões âncora |
| Auth + Rate Limiter (Security) | stable | 2026-06-22 | PLAN-0017 CONCLUÍDO (todas 4 fases) — AuditLog, Stripe sanitization, password reset, Helmet, RLS, pg_audit, DB segregation. **2026-08-18**: `ERR-0067`/`ERR-0068` corrigidos — sessão expirava sempre em 15min (cookie de refresh `Secure` era descartado pelo navegador sem TLS, `SEC-30`/`PLAN-0019`; `refreshAccessToken()` também apagava um access token ainda válido quando a renovação falhava). `TLS_ENABLED` novo controla o `Secure` do cookie (default `false`, sem TLS real ainda). Achado colateral corrigido junto (`ERR-0069`, `admin-docker-status`): bot de status confundia sessão expirada com Docker offline, gerava alarme falso — ganhou estado `unknown` distinto de `offline`. Sem commit/push (aguardando aprovação). |
| Prisma Schema | stable | 2026-06-22 | PasswordResetToken + AuditLog adicionados; passwordHash com doc comment; migration 20260622000000 aplicada |
| Infra Docker | stable | 2026-06-22 | postgres customizado (Debian+pgaudit); jlr_api_rw/ro criados; RLS ativo; init-api-users.sh para fresh deployments |
| Security Hardening Pós-Incidente (PLAN-0018) | stable | 2026-07-05 | PLAN-0018 DONE — escopo original (9 vulnerabilidades, ondas 1-4) 100% concluído, validado com penetration test end-to-end e commitado/pushed (`e01d4ef`). HMAC order tracking, rate limits cupom/concierge, least-privilege DB, RLS 5 tabelas, JWT 15min, timing jitter. SEC-27 formalmente decidido como não aplicável (DECISION-012). Ver ERR-0041, ERR-0042. |
| **SEC-30 — TLS/HTTPS ausente em produção (PLAN-0019)** | blocked | 2026-07-05 | 🔴 CRÍTICO — produção roda HTTP puro, sem domínio/TLS. Provável causa raiz real do incidente que motivou PLAN-0018. Desmembrado para `PLAN-0019-TLS-HTTPS-SETUP.md` (plano próprio, BLOCKED até usuário providenciar domínio — Let's Encrypt exige DNS). Levantar proativamente quando usuário mencionar domínio/deploy. |
| Estoque Multi-Unidade + Vendas + BI (PLAN-0020) | stable | 2026-08-18 | EXECUTADO ponta a ponta — ledger auditável, reserva TTL 20min+sweeper, RBAC unidade fail-closed, venda balcão (PAGO, canal+vendedor+unidade, total server-side), Stripe reserva→confirma, Loja Online (unit 3), KPIs CMV/margem/tops, admin produtos por unidade, venda manual, BI dashboard, SPA Esgotado. Checklist do plano fechado desde 2026-07-07 (**commitado e pushed `ee6a61a`**); testes automatizados 23/23 PASS. **As 2 pendências finais fechadas em 2026-08-18**: validação visual das 5 telas hoje nativas no Admin V2 (Cadastros→Produtos, Operação→Produtos, Operação→Lista→Venda manual, Panorama, SPA) testadas ao vivo via browser real, e pentest manual S10 (16/16 PASS — nenhum vazamento cross-unit em leitura/escrita/BI/endpoint cross-unit, gating por papel confirmado, query string forçando `unitId` de outra unidade sempre ignorada pelo servidor). Dados de teste sempre revertidos. Plano renomeado pra `memory/plans/PLAN-0020-DONE-PRODUTOS-ESTOQUE-LEDGER-VENDAS-MULTICANAL-BI.md`. |
| Infra Docker — build api/web | stable | 2026-07-22 | ERR-0044 corrigido: `NODE_OPTIONS=--dns-result-order=ipv4first` em `apps/api/Dockerfile` e `apps/web/Dockerfile` (IPv6 sem rota na rede Docker causava `ETIMEDOUT` no `npm ci`). Validado com rebuild completo; containers saudáveis. Falta commit/push. |
| ~~Menu Admin + Seções Telas (PLAN-0021)~~ → Superado | superseded | 2026-08-20 | Reagrupamento do menu lateral do Admin **legado** (Equipes-Metas/Perform após Vendas; grupo "Cadastro"; "Seções Telas" etc.) — código já estava commitado (`9422f64`) desde 2026-07-22, só faltava rename `-DONE-`/Git Record. Antes disso acontecer, `PLAN-0033` (mesma sessão, 2026-08-20) removeu fisicamente os 4 arquivos que este plano alterava (`AdminContent.tsx` e cia., commit `1572e31`). Fechado **SUPERADO** por decisão explícita do usuário, sem reexecução — Admin V2 tem menu próprio, entregue em outros planos. Arquivo renomeado `PLAN-0021-SUPERADO-...md`. |
| Admin V2 — Fundação + Operação (PLAN-0022) | stable | 2026-08-15 | Retrofit do painel administrativo (`/admin-v2` em paralelo a `/admin`), branch `feature/admin-v2`. **Ondas 0-9 + RETROFIT-010b concluídas, validadas técnica e visualmente** (baseline; Shell+Scope Engine+Panorama Vivo+Health Score v1; Mapa Vivo da Rede+Diagnóstico da Unidade; Board Operacional de Pedidos+Fluxo; Mapa de Capacidade da Agenda dia×hora; Portfólio Vivo de Produtos; Performance de Serviços; Clientes como Fluxo de Relacionamento; Assinaturas como Saúde da Base; Pipeline de Franquias com migração de schema aditiva + movimentação de etapa via UI). `DECISION-013` registra as regras fixas. **Commitado, pushado e mergeado em `main`** (`28e11c1`/`c00a21d`/`63e28bf`, PR #1 fechado via merge `1479cce` em 2026-08-15). Continuação em `PLAN-0023` (Inteligência). |
| Admin V2 — Consolidação (PLAN-0024) | stable | 2026-08-15 | RETROFIT-020 (Cadastros) + RETROFIT-021 (Sistema) entregues — sidebar do Admin V2 ganha 2 "mundos" novos (hubs de cards) que linkam pras telas legadas via deep-link por hash (`/admin#<view>`, mudança cirúrgica em `admin-shell/behavior.ts`); nenhuma reescrita estética (`DECISION-013` regra #5), nenhum endpoint novo. `Segurança`/`Infra` ficam desabilitados no hub de Sistema (sem tela dedicada no legado). RETROFIT-022 (aposentar o legado) fica fora, sem critério fixado. `tsc`/build/lint limpos (17 erros pré-existentes tolerados, nenhum novo), rebuild Docker + validação visual real (Playwright, 30 checks: 12 deep-links + 2 desabilitados + regressão do clique manual no legado). **Commitado, pushado e mergeado em `main`** (`260ce97`/`f0c165e`/`c7ed771`, PR #1 fechado via merge `1479cce`). |
| Admin V2 — Polimento de UX (PLAN-0025) | stable | 2026-08-15 | 6 itens do usuário entregues: cabeçalho dos kanban com fundo próprio (`KanbanColumnHeader`, Operação/Rede/Crescimento); motivo obrigatório na mudança de etapa do Pipeline de Franquias (migração aditiva `reason` + modal); botões mortos de Google/Facebook removidos do login/cadastro (OAuth não vale a pena agora — zero infra + bloqueio real de TLS); subtítulo explicativo na Rede; contraste da Agenda-Capacidade revisado (mesma lógica vermelho=ocioso/verde=ocupado); "Valor em estoque" exposto no Panorama. **Achado sistêmico corrigido na mesma leva**: os 4 tokens semânticos `state-*` do Admin V2 nunca tinham sido compilados no CSS servido desde a Onda 1 do `PLAN-0022` (`ERR-0049`) — fix aditivo (`tailwind.generated.css`, gerado via Tailwind CLI) corrigiu cor em Health Score/Insight Engine/Radar/Gargalos/Agenda de uma vez. `tsc`/build/testes limpos (134/134 PASS), E2E real + validação visual real com checagem de pixel (não só "parece certo"). **Commitado e pushado pra `main`** (`90a1214`, fechamento do Git Record em `5a12e04`; ambas as autorizações — commit e push — dadas separadamente pelo usuário). Plano renomeado pra `memory/plans/PLAN-0025-DONE-ADMIN-V2-POLIMENTO-UX.md` (achado de sessão 2026-08-18: conteúdo já estava DONE desde 2026-08-15, mas o arquivo nunca tinha sido renomeado — regularizado agora). |
| Cadastros/Sistema nativos no Admin V2 (PLAN-0026) | stable | 2026-08-16 | **14/14 ondas concluídas e validadas de verdade** (E2E real + visual real em todas) — plano inteiro entregue. `DECISION-014` (ACTIVE) substitui a regra #5 da `DECISION-013`. Hub de Cadastros e hub de Sistema 100% nativos, nenhum card aponta mais pro legado. Tier P (7 ondas: Planos/Entrega/Branding/Cupons/Textos das Páginas/Seções Telas/Galeria de Mídias), tier M (3 ondas: Serviços/WhatsApp/Testes) e tier G (4 ondas: Produtos/Clientes/Profissionais/Usuários) fechados. 6 bugs reais achados e corrigidos ao longo do plano: `ERR-0050` (schema Zod de cupom), `ERR-0051` (CSS estático), `ERR-0052` (z-index de modal aninhado), `ERR-0054` (colisão de slug de rota, Clientes), `ERR-0055` (fuso horário em data-pura-UTC, Profissionais); `ERR-0053` (backend — produto com histórico de estoque não pode ser excluído) documentado, fora de escopo. Componentes reusáveis nascidos no plano: `DeleteConfirmModal` (tone/confirmLabel), `CategoryStatusManagerModal` (entity service\|product), `HubCard` (modo native), tabela de lookup de breadcrumb. **Onda 14 (Usuários)** fechou o desmembramento de "Pessoas" (`DECISION-014` regra #3) — única das 3 entidades com CRUD completo (Clientes sem DELETE, Profissionais sem POST); gate de papel client-side (só MASTER atribui MASTER) e bloqueio de auto-exclusão espelhados do backend; achado de contrato documentado (não corrigido): troca de papel via form genérico não passa pela rota `/role` auditada. Legado (`apps/web/src/modules/admin-*/`) permanece intocado (`DECISION-014` regra #6 — aposentadoria fora de escopo). **Commitado e pushado pra `main`** (`3961a1c..efd1f45`, 14 commits, um por onda; push autorizado explicitamente pelo usuário em 2026-08-16). Plano renomeado pra `memory/plans/PLAN-0026-DONE-ADMIN-V2-CADASTROS-SISTEMA-NATIVOS.md`. |
| Admin V2 — Inteligência (PLAN-0023) | stable | 2026-08-15 | Continuação do `PLAN-0022`, mesma branch. **Ondas 1-7 concluídas e validadas de verdade (E2E real + visual)**: Radar Executivo (RETROFIT-011), Gargalos (RETROFIT-012), "Onde está o dinheiro?" (RETROFIT-013), Comparador de Unidades (RETROFIT-014), Health Score evolução (RETROFIT-017), Insight Engine (RETROFIT-018, `/admin-v2/insights`, consolida Radar+Gargalos+Comparador com dedup por categoria) e Ações Recomendadas (RETROFIT-019, catálogo de sugestões em texto por categoria, botão real só quando existe tela). Validação das Ondas 6-7 fechada nesta sessão: `docker compose build` + E2E real (login MASTER, dedup/ordenação/`totalKnownImpact` conferidos, regressão OK em todos os endpoints de inteligência) + visual real via Playwright headless (extensão Chrome indisponível nesta máquina) incluindo cliques reais Panorama→Insights→Pipeline. `npm run test` (api) 162/162 PASS. **Ondas 1-7 commitadas, pushadas e mergeadas em `main`** (PR #1 fechado via merge `1479cce` em 2026-08-15) — nenhuma pendência restante nesta leva. |
| Admin V2 — Fixes pontuais (PLAN-0027) | stable | 2026-08-17 | **Conteúdo fechado por instrução do usuário** — 11 itens resolvidos ou encaminhados: Cadastro > Clientes vazio (`ERR-0056`, backfill 8 clientes reais); sessão expirando em 15min (`ERR-0057`); Cupons sem filtro de data; ID somente-leitura nos forms de Clientes/Serviços + reordenação Email/ID de usuário (`ERR-0059`); legenda no Panorama > Clientes; 2 cupons de teste; filtro "Destaque" em Serviços + 8 serviços reconciliados com os flip-cards (`ERR-0060`); `Membership.imageUrl` novo (`ERR-0061`); Panorama contando cliente físico 2x corrigido por decisão do usuário (`identityKey` telefone > email > nome, `ERR-0058`, 17→9 clientes validado real). Os 2 achados de arquitetura (religar flip-cards de Serviços e cards de Planos pro cadastro nativo) foram **desmembrados para `PLAN-0028`**. `tsc`/build/testes (134/134) limpos. Sem commit/push (Pre-commit review pronto no plano, aguardando aprovação). Arquivo não renomeado `-DONE-` até o Git Record ser completado (regra do kernel). |
| Kanban drag-and-drop — Operação + Franquias (PLAN-0029) | stable | 2026-08-17 | **Executado e validado** (E2E real + visual real via Chrome). Componente reusável novo (`KanbanDndBoard.tsx`, `@dnd-kit/core`) usado nos dois boards. Operação: drag `emPreparacao`↔`prontos` (colunas `entraram`/`atencao` fixas, alerta/estado automático não é arrastável); solta em "Prontos" grava `DESPACHADO`, em "Em Preparação" grava `SEPARANDO`; reusa `PATCH /orders/:id/fulfillment` (legado, sem mudança). Franquias: `<select>` removido, card inteiro arrastável entre qualquer etapa, mesma regra de negócio de antes; soltar abre o modal de motivo obrigatório já existente. `DECISION-015` substitui formalmente a regra "usuário nunca arrasta" de `PLAN-0022`/RETROFIT-010b. `ERR-0064`. `tsc`/build/testes (134/134) limpos. Sem commit/push (Pre-commit review pronto no plano, aguardando aprovação — mesmo lote do `PLAN-0027`/`PLAN-0028`). |
| Religar conteúdo público — Serviços e Planos (PLAN-0028) | stable | 2026-08-17 | Desmembrado do `PLAN-0027` (Itens 10/11); **executado e validado** (E2E real + visual real via Chrome). Caso A (Planos): `imageUrl` renderizado no grid de Planos, já data-driven via `index.behavior.ts` (correção de uma afirmação errada do fechamento do `PLAN-0027`). Caso B (Serviços): 5 campos novos em `Service` (`highlightLabel/Tagline/BackLabel/Description/Order`, migração aditiva); endpoint novo `GET /public/services/featured`; `HomeServicesSection.tsx` reescrito data-driven (visual 1:1 preservado); campos novos no `ServiceFormModal.tsx`; conteúdo dos 9 flip-cards migrado via `seedServiceHighlights.ts` — decisão do usuário: card duplicado ("Limpeza de Pele" em 2 cards) resolvido trocando um deles por `Drenagem Linfática Facial`, mantendo os 9 cards com serviços distintos. Achado técnico corrigido na mesma leva: binding de clique do "Agendar" convertido pra delegação (necessário pro render assíncrono novo). `ERR-0062`. `tsc`/build/testes (134/134) limpos. Sem commit/push (Pre-commit review pronto no plano, aguardando aprovação — mesmo lote do `PLAN-0027`). |
| Board Operacional de Pedidos — fluxo real de 5 etapas (PLAN-0030) | stable | 2026-08-18 | **DONE, commitado e pushado** (`origin/main` `6ccb472..3e5d82a`, 2 commits `c7160da`+`3e5d82a`, aprovações separadas de commit e push). Kanban de Operação > Pedidos passa de 4 colunas pras 5 reais e sequenciais: Recebido→Pago→Em Separação→Pronto→Despachado/Entregue, nunca pula etapa. Motivo da reformulação: achado ao vivo (`ERR-0066`) — destravar só o `fulfillmentStatus` de "Entraram" não movia o card (coluna decidida por `Order.status`, não fulfillment). Recebido→Pago exige confirmação manual (nome+data, `ConfirmPaymentModal`, grava em `OrderStatusHistory.note`) atrás de uma flag (`Setting` `operations.manualPaymentConfirmationEnabled`, default ligada, desliga sem redeploy quando integração de pagamento real existir — validada ao vivo). Pronto→Despachado/Entregue abre `ConfirmDispatchModal` (Entregue direto; Despachado pede meio+data editável/retroativa); um pedido `ENVIADO` continua arrastável ali — soltar de volta na própria coluna confirma `ENTREGUE`. Zero migration, zero endpoint novo — só campos opcionais em `PATCH /orders/:id` (`note`) e `PATCH /orders/:id/fulfillment` (`shippedAt`). Grid sem scroll horizontal (`xl:grid-cols-5`). "Atenção" **não é uma 6ª coluna** — é um agregado (`count`/`totalValue`) que vira banner de resumo no topo + selo inline por card (`card.reason`, já existia); alimenta Gargalos/Radar sem mudança neles. Chegou a existir uma versão intermediária com "Atenção" como coluna exclusiva arrastável (commit `c7160da`) — usuário testou ao vivo, reportou "todos voltam" (os drags funcionavam de verdade, só que o card reaparecia no mesmo lugar por causa da exclusividade), corrigido no mesmo dia (commit `3e5d82a`) removendo a exclusividade. `DECISION-016` (item 6c é o desenho final; 6b registrado como superado). Sub-item independente: `ERR-0065` — tooltip do Motivo em Franquias trocado de nativo pra próprio, com fallback "N/A". `tsc`/build/testes (134/134) limpos em toda a leva. **Follow-up mesma sessão (`ERR-0070`)**: `xl:grid-cols-5` nunca tinha sido compilado no CSS servido (`tailwind.generated.css` é foto estática do Tailwind CLI, mesma causa raiz do `ERR-0040`/`ERR-0049`/`ERR-0051` — usuário reportou o board quebrado, 2 colunas largas); CSS regenerado + `lg:grid-cols-3` adicionado ao grid. Commitado/pushado junto com o `PLAN-0031` (`322f636`). |
| Lista de Pedidos — migração nativa de "Pedidos e Vendas" (PLAN-0031) | stable | 2026-08-18 | **DONE, commitado e pushado** (`origin/main` `3e5d82a..322f636`, commit `322f636`, aprovações separadas de commit e push). **Executado e validado, 7 ondas** (E2E real via Chrome + Postgres real). Nova aba "Lista" dentro de Operação (`/admin-v2/operacao/lista`), irmã do Kanban: tabela completa com busca/filtro/paginação/seleção múltipla + 7 KPIs, modal de detalhe (somente leitura — itens/pagamentos/histórico), modal de edição (status+fulfillment/transportadora/rastreio/notas, bloqueia opções que avançam etapa sem pagamento aprovado), ação em lote "marcar próxima etapa", venda manual/balcão completa (catálogo produto+serviço, disponibilidade cross-unit). **Zero mudança de backend** — os 5 endpoints usados já existiam pro admin legado (`GET /orders`, `GET /orders/summary`, `PATCH /orders/bulk/advance`, `PATCH /orders/:id`, `POST /orders`), migração 100% frontend (ilha React+JS imperativo virou React declarativo de verdade). Link "Ver no Admin →" dos cards `BLOCKED` do Kanban religado pra cá com `?highlight={id}` — abre o pedido específico direto, melhoria real sobre o legado (nunca teve deep-link). Checklist do `ERR-0070` aplicado proativamente (`min-w-[200px]` checado/regenerado antes de dar como validado). Botões de paginação ganharam `bg-primary` (mesmo estilo do "+ Venda manual") — contraste baixo demais como estavam. `tsc`/build (web) limpos; `npm run test` (api, 134/134) PASS sem mudança (backend intocado). |
| Revalidação guiada — foco Estoque/Ledger (PLAN-0032) | stable | 2026-08-20 | **DONE**, 6 ocorrências tratadas ao vivo, uma por vez, cada uma validada (browser real + rebuild Docker, ou `curl`/Postgres direto) antes de seguir. Ver `PLAN-0033` (linha abaixo) pro achado #6 (Admin legado) virar aposentadoria de verdade. **(1)** `ERR-0071` — `POST .../stock/adjust` pra cima podia falhar com falso "estoque insuficiente" (tratava AJUSTE como saída dentro de `applyStockMovement`); `applyStockAdjustment` novo faz a escrita direta, sem passar pela checagem de saldo negativo de entrada/saída; `StockHistoryModal.tsx` ganhou coluna "Saldo anterior" derivada em runtime + sinalização visual de inconsistência linha a linha (a causa do relato original era só falta dessa coluna + ordenação mais-novo-primeiro, ledger em si já batia certo); cache `Product.stock` resincronizado em 2 produtos que tinham dessincronizado por reversões manuais de sessões anteriores. 5 testes novos, 167/167 PASS. **(2)** contorno dos 134 campos reais (input/select/textarea) do Admin V2 inteiro trocado de `border-gold/40` pra `border-primary/60` (1px, ajuste fino ao vivo revertendo um 2px inicial). **(3)** Operação→Lista: resumo de 7 indicadores em 1 linha só (nomes completos, Receita por último), paginação movida pra logo após os filtros; mesma paginação adicionada à grid de Produtos (nunca tinha). **(4)** Detalhes do Pedido reformulado (`@frontend-specialist`) — chunking por seção, faixa de destaque status+total, timeline no histórico, 100% tokens de marca existentes. **(5)** "Onde está o dinheiro?" reformulado (`@frontend-specialist` + skill `dataviz`) — cascata+origens viraram 6 stat tiles coloridos (paleta validada com `scripts/validate_palette.js`, ALL CHECKS PASS), as 6 decomposições (unidade/produto/serviço/canal/profissional/plano) viraram abas com painel de altura fixa + rolagem vertical. **(6)** auditoria de fechamento: 12/15 telas do Admin legado já têm equivalente nativo; Metas/Performance sem model/API real (nada pra migrar); Assinantes é o único gap funcional de verdade (backend `POST`/`PATCH /subscriptions` pronto, zero UI). `tailwind.generated.css` regenerado 5x (checklist `ERR-0070`/`0071` seguido proativamente em toda ocorrência visual). `apps/api` `tsc -b`+`npm run test` 167/167 PASS; `apps/web` `tsc -b`+`npm run build` PASS. Commit/push autorizados explicitamente pelo usuário. |
| ~~Admin legado (`/admin`, 24 módulos)~~ → **Aposentado (PLAN-0033)** | deprecated | 2026-08-20 | **Removido da base local**, `DECISION-017` autoriza (supera `DECISION-013` regras #1/#3). Brainstorm de abordagem feito antes (`@project-planner`, 3 opções — big bang escolhido). 6 ondas: **(0)** branch de arquivo `archive/admin-legado` criada e pushada pro GitHub, rede de segurança antes de qualquer remoção. **(1)** widget de status de infra (API/Postgres/nginx) portado pro Admin V2 — `DockerStatusButton.tsx` novo, ícone pequeno no topbar, mesma lógica de `useDockerHealth` relocada. **(2)** botão "← Admin" virou "← Voltar", aponta pra SPA pública. **(3)** os 2 specs E2E acoplados ao DOM legado (`flows.spec.ts` "Admin flows", `membership-grid.spec.ts`) reescritos contra o Admin V2 nativo — achada e corrigida uma cadeia de 4 bugs pré-existentes na suíte (`ERR-0072`: `unitId` faltando, 6 enums em inglês vs. schema PT-BR, agendamento sem vínculo profissional-serviço/turno). **(4)** remoção física: 82 arquivos (24 módulos + `AdminContent.tsx` 709 linhas + `pages/Admin.tsx` + rota `/admin`) — `tsc -b` compilou limpo de primeira, confirma zero acoplamento real. **(5)** validação final: bundle **1.435→901 KB (−37%)**; Admin V2 inteiro navegado ao vivo sem quebra; card "Infra" do hub Sistema corrigido (citava o modal do legado, já aposentado); 3ª falha de E2E fora do escopo original (`order-dashboard-lifecycle.spec.ts`) corrigida a pedido do usuário (`ERR-0073`, mesma classe do `ERR-0072`); suíte final 4/5 verde (1 falha pré-existente não relacionada, carrinho público, não tocada). Backend (`apps/api`) 100% intocado, 167/167 PASS confirmado. Dados de teste (produtos com histórico de estoque, `ERR-0053` bloqueia `DELETE` via API) limpos do banco real via SQL direto a cada rodada de validação. **DONE, commitado e pushado** — leva 1 (Ondas 0-3) `94c016e`, leva 2 (Ondas 4-5) `1572e31`, ambas com aprovação explícita e separada. Plano renomeado pra `memory/plans/PLAN-0033-DONE-APOSENTAR-ADMIN-LEGADO.md`. |
| Auditoria de Textos/Imagens Hardcoded pós-Admin V2 (PLAN-0034) | stable | 2026-08-24 | **DONE, commitado e pushado** (`964ae52`), fechado formalmente nesta sessão (trabalho técnico concluído em 2026-08-20). Objetivo de portabilidade SaaS/white-label (`DECISION-018`). 7 fases: 7 achados de texto + 2 de imagem migrados pra `pageTexts`/`mediaSlots`; 2 correções de nomenclatura no Admin V2; 45 chaves/slots órfãos removidos do código (banco intocado, `DECISION-019`); editores revalidados ao vivo (Docker rebuild real); 3 achados de branding "JLR" hardcoded corrigidos + 1 bug real fora do escopo original (`ERR-0074` — número de WhatsApp divergente entre `CheckoutContent.tsx`/`index.behavior.ts`, unificado via `branding.whatsappPhone`). `tsc -b`/build/test limpos (`apps/api` 134/134 PASS). |
| Auditoria de Qualidade do Admin V2 — code-archaeologist (PLAN-0035) | stable | 2026-08-24 | **DONE, commitado e pushado**. Auditoria via `@code-archaeologist` (4 subagentes em paralelo) sobre `apps/web/src/admin-v2/` inteiro (116 arquivos, 17k linhas) — 14 achados (1 crítico + 6 médios + 7 baixos). 13 bugs corrigidos no total (`ERR-0075` a `ERR-0088`): fulfillment (rastreio/notas) descartado silenciosamente pela UI; validação `NaN` ausente em campos de comissão/custo; "reverter logo" salvando o formulário inteiro no Branding; feedback verde/vermelho decidido por substring match no WhatsApp; teste de gravação podendo deixar registro órfão; condição de corrida no drag do Kanban de Pedidos (`movingOrderId`→`Set`); `updateBranding` mascarando payload malformado; contagem "sem pagamento aprovado" dependente de texto (`reasonCode` estável adicionado ao backend); bloco JSX duplicado extraído (`KnownImpactBanner`); e o erro de lint `react-hooks/set-state-in-effect`, que na investigação se revelou presente em **27 arquivos** do repo (não só os 11 do escopo original), todos corrigidos (adiar 1 tick via `setTimeout`) + 1 caso de `react-hooks/static-components` (componentes movidos pra fora do componente pai). **Lint do `apps/web`: 28 → 0 erros.** Achado #14 (hook compartilhado `useAdminV2Fetch`, DRY dos 11 dashboards) fica **deferred**, não é bug, disponível pra retomar. Efeito colateral: `checklist.py` do kernel SFK corrigido (`ERR-0088` — caminho `.agent/` inexistente + `python` inexistente neste sistema); `DECISION-020` registrada (múltiplos `<h1>` nas seções públicas do site são intencionais — arquitetura de seções independentes, não bug). |

---

## Technical Debt

<!-- Severity: critical | medium | low -->

| Area | Debt | Severity |
|------|------|----------|
| ~~API startup~~ | ~~Migration race condition~~ | ~~low~~ — **RESOLVIDO** docker-entrypoint.sh |
| ~~Seed~~ | ~~MASTER_EMAIL conflita com adminEmail~~ | ~~low~~ — **RESOLVIDO** seed reordenado |

---

## Recent Activity

<!-- Log of completed tasks. Summarize entries older than 30 days. -->
<!-- Format: - YYYY-MM-DD: [what was done] (PLAN-XXXX or point-in-time) -->
- 2026-06-10: Cópia do projeto JLR_AI_Studio → JLR_Beauty, git init, npm install (PLAN-0009 DONE)
- 2026-06-11: Docker Compose + PostgreSQL + Dockerfiles + nginx + migration PostgreSQL (PLAN-0010 validado)
- 2026-06-11: Refactor routes/index.ts God File → 9 arquivos de domínio + 6 libs (PLAN-0011 validado)
- 2026-06-11: Page Texts Editor — 129 campos, 11 componentes migrados, Admin UI (PLAN-0012 CONCLUÍDO)
- 2026-06-12: Docker Status Modal + fix nginx boot/inode/proxy_pass (PLAN-0013 em andamento)
- 2026-06-13: About em Franquias + MissionSection global (PLAN-0014 em andamento)
- 2026-06-16: Franquias Page Upgrade — 13 novas seções, 13 componentes TSX, ~145 page texts, ~26 media slots, 13 toggles (PLAN-0015 DONE)
- 2026-06-20: Unified Nav — menu único Home/Assinaturas/Franquias, 4 dropdowns, 7 landmarks (PLAN-0016 DONE)
- 2026-06-21: Fine-tuning Franquias — bg A/B, imagens contain, Founder circle, cards modelos (point-in-time)
- 2026-06-21: Security Fase 1 — rate limiter PostgreSQL, requireMaster/Staff, email-only login (PLAN-0017 em andamento)
- 2026-06-22: Security Fase 2 — refresh token, logout, emailVerified enforcement, verify-email flow (PLAN-0017 em andamento)
- 2026-06-22: Security Fase 3 — Helmet, DB segregation jlr_api_rw, RLS, pg_audit, health endpoints protegidos (PLAN-0017 em andamento)
- 2026-06-22: Security Fase 4 — AuditLog, Stripe payload sanitization, password reset flow, passwordHash comment (PLAN-0017 CONCLUÍDO)
